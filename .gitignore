# Python caches
__pycache__/
*.pyc
*.py[cod]
*.pyo
.ipynb_checkpoints/

# Virtual envs
.venv/
venv/
env/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# Tool caches
.mypy_cache/
.pytest_cache/
.ruff_cache/

# local reproduction scratch (regenerable prediction CSVs, ~235MB)
results/repro/

# large/regenerable data + run outputs (caches, chunk dirs, plots) — keep results/moe_ladder/ (small tables)
results/resid_prep/
results/covid_imp_rank/
results/ebm_resid-*/
results/resid_ab/
results/date_reconstruct/
results/intraday_feats/
results/*.png
logs/
*.tgz

# local config / cluster run-state (not code)
.claude/
.hpc/campaigns/*/*.lock
.hpc/campaigns/*/drive*.log
.hpc/campaigns/*/drive_state.json
.hpc/campaigns/*/driver_stdout.log

# synced per-run OOS predictions (regenerable; large) — notebooks recompute from these when present
results/moe_ladder/preds/

# every-bar window-ablation per-bar losses (large; regenerable by re-harvesting from the cluster / winablate_r1.py)
results/winablate_full/*_lossbar.npz
