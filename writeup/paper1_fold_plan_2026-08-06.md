# Fold plan — merging the alpha-manifestation session into Paper 1 (author decision 2026-08-06)

Decision on record: the session folds INTO main.tex (not Paper 2); no .tex edits yet — this
plan is the map for the joint paper pass. Existing arc: S1 kernel backbone → S2 bucket
attribution → S3 dense-weak/shrink-never-select → S4 low-rank operator → S5 portable
five-question state + F1–F4 (per `paper_restructure_2026-08-01.md`).

## Proposed expanded arc

- **S1–S2 unchanged** (kernel backbone, bucket attribution). Session touches nothing here.
- **S3 gains its mechanism and its ledger.** Add: (i) the gauge-degeneracy result (§27 C5:
  dependence-adjusted MP edge keeps 5/106 — "sparse in basis, not low-rank in data space" now
  has a cause: the basis is unpinnable); (ii) the casualty ledger grown to 15 (re-draw included:
  frozen-2005 beats refreshed-2022 ON the era the coupling rotated, −2.79/−2.41); (iii) the
  kNN resolution demanded by PAPER_CRITIQUE: three-view head-to-head, eigenmap worst
  (−10.77), corrections hurt a strong base — replaces the criticized selling-by-omission
  section with an honest boundary statement.
- **S4 gains an independent replication.** The operator story ("static geometry, fast
  amplitudes") re-derived on a second basis with different machinery: frozen frame + coupling
  map (+0.62) + daily rotor (+0.79, 35d, phase forecastable ~1 month) + intraday circulation
  (+0.992, no clock, kill-tested). Present as a subsection: "the same law from the state side."
  PANEL note: this is the honest resolution of the one-panel commitment — the session panel is
  a REPLICATION basis, never mixed into S1–S5 tables.
- **NEW S5′ (before the current S5): amplitude vs gain.** §31 (amplitude forecastable R² 0.485,
  gain not), the §34 regime atlas (A3 daily-only labels win; A4 intraday fail — the boundary),
  and §34.2's conditional smear (second-moment conditioning improves QLIKE +3.58, ablation
  pending) — this is F1 ("innovations not levels") upgraded to a quantitative law with a
  production consequence.
- **S5 (portable state) gains two facts:** the transmission result (antisymmetric lag-1
  structure is real feature content — raw-lag control failed, D-weighted passed +2.58) and the
  cadence decomposition at product horizon (linear ~85%, products +1.16…+3.08 Shapley bracket).
- **NEW S6: horizons and the product** (was banked for Paper 2 — now in): the fast-edge curve
  (+5.42/+2.77/+1.23), mixed-horizon training pollution and the +3.24 open-slice resolution,
  α-law (H=16 +2.26), instruments in production (rot detector: right alarm, wrong remedy),
  VRP framework (spec + Tier-1 machinery; numbers pending data).
- **F-facts extended:** F5 amplitude-forecastable-gain-not; F6 frozen-beats-refreshed (frame,
  products); F7 the dense edge is fast in horizon.

## Battery comparability (correction 2026-08-06)

The causal-tune battery ran per-bar (cadence matched — a stronger control than first credited)
but on the PRE-§16 panel; the final model's numbers are on the fixed panel. **No merged ladder
until AM-13 (battery rerun on the fixed panel + tree parity rematch on the 699 feature set)**.
Until then the paper presents the battery as attribution on its own panel, the final model on
the fixed panel, and never joins the tables.

## Gating items before the paper pass

1. AM-10 Romano–Wolf sweep over the new family (phase sliver, cadence-H8, open-slice, α-law
   H16, transmission, A3, conditional smear) — final Tier-2 grading. All members now landed
   except the kNN rerun (leak-fixed, in flight) and the smear ablation (labels vs trailing-only).
2. Smear ablation (§34.2): attribute +3.58 between conditional trailing means and regime labels.
3. Panel-unification wording (replication-basis framing above) — author sign-off.
4. Tables: session numbers quoted only in replication/S6 sections, never merged into b2 tables.
5. LSTM seat (P3, §33) — not yet run; decide in the paper pass whether it enters S3's
   nonlinearity story or is dropped to future work.

## Critique items this session resolves (PAPER_CRITIQUE_2026-08-02)

- Spectral-kNN selling-by-omission → replaced by the three-view head-to-head + prior record.
- "Trails every causally fitted arm" (conclusion.tex) → now backed by three independent
  confirmations including on the strongest deliverable.

## Source of numbers

`writeup/CLAIMS_LEDGER_2026-08-06.md` (graded), `alpha_manifestation_findings_2026-08-04.md`
(sections), `analysis/README.md` (module map). Every quoted DM/QLIKE traces to a § and a module.

## Addendum (same day, post-§37–40): what the width/span campaign changes in the arc

Gating-list corrections: AM-10 sweep DONE (§36, 5/6 survive, phase dies); smear ablation DONE
(§34.8 family sweep: leverage + probe survive); kNN rerun DONE (leak-fixed, FAIL — the §33
boundary statement stands). Outstanding gates are now: the AM-15 closing Romano–Wolf sweep
over the post-§36 family, and AM-13 (cluster-side battery/tree rematch).

New content and where it folds:

- **S4 (operator story) gains the width campaign as its quantitative capstone:** the
  transmission plateau (§38/38.1/38.1b: flat 30–80, cliffs both sides), the per-bar entry
  (+4.59, §38.2), arrow replication (+0.838 new block, §38.4), the horizon-locality of width
  (§39a: h = 1-only), and the §38.3 mechanics (payload = the new directions' own motion; the
  tail is genuinely empty; no shrinkage or trail tuning helps). One paragraph-sized law:
  "the lead-lag field is finite-rank ≈ 2× the static frame, fast, stable, and robust to
  truncation anywhere on the plateau."
- **S5′ (amplitude vs gain) gains its resolution:** §34.13's "predictability persists,
  complexity died" + §34.10b/d (union edge era-bounded; window/cadence laws do NOT govern
  the second moment) + the §34.10 span verdict with its era boundary. The channel law's
  final wording: the mean is dense and lawful under recency; the variance is means-sufficient
  on the modern regime, and its historical extra span was calendar + slow aggregates
  (§34.10e).
- **S6 (horizons/product) gains the per-horizon production table** (h=1: 719 per-bar +
  smear, composed 0.12303; H=4/8/16: 679+F20 with α-law at 16; EOD: 679, constant smear by
  measurement — §34.14 casualty #16) and the term structure with the H=16 α-law revival.
- **Casualty ledger: 16** (re-draw #14, cross-time products #15, EOD smear #16).

PANEL note unchanged: all of this stays in the replication/S6 sections; b2 tables untouched.
