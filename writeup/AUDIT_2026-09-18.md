# Audit ledger, 2026-09-18 session (11:00 delta-hedged SPX 0DTE straddle line)

Scope: everything done in the worktree `harxhar-0dte-professor` (branch `grok/0dte-professor-notes`) from the end of
the Grok session (2026-09-17 23:51) through 2026-09-18. Four read-only audits (process, methodology, code, missed
ideas); full reports in the session scratchpad (`PROCESS_AUDIT.md`, `audit_method/METHOD_AUDIT.md`,
`audit_code/CODE_AUDIT.md`, `audit_ideas/MISSED_IDEAS.md`). Nothing is committed. Items marked FIXED below were
fixed after the audit; everything else is as found.

## 1. State of the tree
- HEAD unchanged (2016e4b, 2026-09-10). 84 paths staged (the d3809a4 cherry-pick); about 280 dirty or untracked paths
  mixing: Grok's 2026-09-17 work (intraday notebook line, DH index PDFs, standalone), the four DH fixes with regenerated
  `results/spxw_pnl/` and `writeup/generated/options_numbers.tex` (68 of 224 macros moved, headline unchanged), the
  standalone's MaxDD columns, notebook section 11, proposals 27-41, the `live/ibkr/` package, `results/live_*`.
- Outside the repo: `ib_async` installed in envs/285J; PowerShell `claudex` now `gpt-6-astra` (no backup made);
  CLIProxyAPI left running on port 8317.

## 2. Where we tripped (ranked)

### Conclusion-changing
1. Every "improvement" in proposals 30-38 was scored on an uncosted, unlagged book. Proposal 27 showed the crossed
   exit book nets Sharpe 1.20 at 0.5 bp hedge cost (era), about 0 at 1 bp or with a one-bar lag. The two surviving
   positives (36's bias-corrected delta V9, 38's V14) raise turnover; at 0.5 bp they net about 1.31 and 1.37 against
   the book's 1.20. Every headline should be restated net of hedge cost.
2. Session-wide multiple testing. About 830 inferential cells in one night. On the rule-adoption gates (30, 31, 33,
   35B, 37) 576 cells produced 18 passes where 28.8 are expected under the null: fewer adopted rules than chance. No
   rule pass survives Holm within its own study. Two results survive correction: the combined next-bar forecaster (35A)
   and the bias-corrected delta V9 (36; survives Bonferroni across all 24 hedge-volatility variants).
3. The hedge-density mechanism is not established. Proposal 39's "denser hedging pays more gamma" rests on the sum of
   squared one-minute returns (index panel) exceeding the squared 30-minute move (vendor spot tape) by 5-16 percent:
   two price sources, so a microstructure or stale-print mismatch is as likely as within-bar mean reversion. The
   primary-book bootstrap already said "no evidence". Retract "30 minutes is near-optimal because of mean reversion";
   keep "no evidence finer hedging helps, and turnover cost dominates".
4. Proposal 37 was settled by algebra. f_comb minus slice equals w times (rv_hat minus slice) with w positive on every
   day, so the combined sign rule is the ridge rule; the same identity made 35B's zero-margin cells redundant. Its
   passes (XGBoost, LightGBM sign rules) are proposal 30's forecast zoo re-screened; a different forecast wins each study
   (a0 in 30, xgb and lgbm in 37, blk2 nowhere), the signature of noise selection.
5. The live package is unsafe as written (code audit): an unfilled exit combo still flattens the futures (naked into
   settlement), and that is the expected path because the crossing cap ($0.10) is below the fixture's spread and the
   option tick is wrong above $3 (0.10, not 0.05); partial fills are lost and mis-signed; the kill switch marks futures
   at the index; a non-invertible vol silently unwinds the whole hedge; `--mode paper --port 7496` reaches live; four
   DaySummary defects; the restart guard swallows exceptions (double-entry risk); no snapshot timeout. The 58 green
   tests cannot see any of it. README's "ES at four straddles equals MES at one" is false (parity needs n=10).
6. Professor-facing standalone: the exit signal used the raw censored IV (NaN on the five March-2020 days, so every
   model silently held; FIXED, see section 6); the "crossed" column charges no spread on the 15:30 buy-back and marks
   it at a model price about 0.1 point below the quoted mid (roughly the whole crossed advantage of the exit rules);
   the gate covers the hold row, not the headline exit rows.
7. The premium-unit convention is a 5:1 inverse-premium sizing rule. Per contract the exit book is Sharpe 1.81 whole
   and 2.29 era (not 2.25 and 2.51); the worst day in points is 2022-10-13 (minus 39 points), not 2024-04-04.
8. 419 unused sessions. The chain and spot file run to 2025-12-31; the sample stopped at 2024-04-30 because the
   forecast panels do. The forecast-free always-short book can be replayed out of sample today (proposal 41).

### Number-changing
9. The gate constant 2.2525467549928 is a float32 leg-sum artifact (27 sums bid and ask as float32); the book is
   2.2525467094 whole and 2.5131321677 era; 30 asserts the wrong constant to 12 decimals; 32 mis-attributed the 4.6e-8.
10. 31's "size by margin" row is unbounded (positions to 25,591x); it loses anyway.
11. 29's fee line charges commissions only; the MES half-spread (about $30 a day at 48 contracts) is charged nowhere;
    the MES exchange fee is an unsourced midpoint; dollar P&L uses mean(r) times median(premium).
12. 38's V14 has no constant-scalar control and its lambda grid is monotone to the boundary; "a second constant" is
    the honest reading.
13. 28's replays freeze IV, which makes the delta a step function; 28 and 32 disagree on whipsaw versus trend only
    because of that convention (32 is the tape).
14. 30's VRP-proxy rule is mislabelled (full-day RV over a 30-minute slice); the result is unchanged.
15. Day-set mismatches in prose: 865 versus 797 (walk-forward warm-up) versus 489; 35A common cells 803 days and
    9618 bars; era terciles cut on the full sample (156, 164, 169).

### Process (self-audit, abridged; full list in PROCESS_AUDIT.md)
16. Applied a 118-file port on a misread question (reverted). Told agents the hedge charged 0.5 bp (it charged
    nothing). Loose numbers corrected later (0.38 versus 0.42 percent premium; log versus simple move; "no March 2020
    in sample"). Designed 34's unfair target and 37's algebraically empty rule. Never rolled up the multiple-testing
    count. Thirteen studies in one night outran review. 84 files left staged; no commit plan offered until asked.
    Package installed and proxy started on my initiative; profile edited without a backup.

## 3. What is solid (tried to break, could not)
- Causality: no leak found in any rule, weight, profile or calibration (w_slice lag, 35's weights, 36's canary, 30
  and 31's embargo); no rule reads a full-sample quantile.
- The four DH fixes match spec exactly; the copied everybar input is the right file by md5.
- The engine parity (2e-13 exit, 1e-15 hold) and the gate chain across studies (apart from item 9).
- The anatomy (32): net = theta 0.574 minus convexity 0.390 minus vega 0.052 minus friction 0.087 = +0.046; losses are
  one large afternoon bar on trending days (tercile Sharpe +6.96, +4.24, minus 1.95; not a premium-level artifact).
- The negatives: skip rules, margins, stops, exit ladder, delta band, calendar filters, forecast-vol deltas, per-bar
  toggling, wings for the exit book, 60-minute cadence.
- 35A: the combination beats ridge and slice on next-bar QLIKE (0.151 versus 0.183 and 0.181 whole; 0.139 versus
  0.184 and 0.161 era).
- 36: V9 (+0.18 Sharpe era, survives correction) and its refutation of its own premise (an honest rest-of-session
  model still loses to the implied). Repo defect found: two different objects are both called the "remaining forecast".

## 4. What we missed (ideas audit; in-sample and descriptive until gated)
- Skip the rich days, not the cheap ones (top quintile of 11:00 premium over spot earns about 0 per unit; a causal
  80th-percentile rule lifts the exit book 2.25 to 2.40 whole and halves the max drawdown; the sat-out days lose
  money). One pre-registered cell.
- Ride the OTM leg at 15:30 (buy back only the ITM leg): exit 2.29 to 2.65 whole and 2.51 to 2.75 era in sample, at
  the price of one-sided settlement exposure (proposal 41 with its stress grid and the 419-session OOS).
- Per-contract accounting alongside premium units, everywhere.
- Charge the hedge cost in the book of record; add a constant-scalar control to the delta studies; charge the MES
  spread.
- Killed with numbers: later entry (11:00 is best), pin-risk exit, 15:30 wings, magnitude sizing, 15:30 re-sell.

## 5. Recommended order (slow)
1. Commit or discard by unit: (a) DH fixes and regenerated outputs; (b) standalone and notebook; (c) studies 27-41;
   (d) live package. Decide (b) after section 6 is reviewed.
2. Standalone: charge or footnote the exit spread; add a per-contract row; extend the gate to the exit rows.
3. Live package: the safety batch (item 5) as one reviewed unit; no paper session before it.
4. Restate every headline net of 0.5 bp hedge cost and per contract; retract the density mechanism; withdraw the
   "three constructions agree" sentence in 32's report; fix 30's gate constant.
5. Await proposals 40 (cadence by clock) and 41 (OTM leg and OOS 2024-05 to 2025-12); then the rich-day skip as one
   cell.
6. Paper story: the premium curve by clock and year, the harvest decomposition, the combination weights, all costed.

## 6. Fixes applied after the audit
- 2026-09-18: `writeup/make_dh_causal_standalone_tex.py`: the 15:30 exit signal now uses the re-inverted hourly IV
  (`deck_frame()` shared with `hold_mark_1100`), so the five censored March-2020 bars carry a signal instead of a NaN
  that forced a hold. Table and PDF regenerated: hold row unchanged (4.20 / 3.58); every model row moves by 0.01-0.03
  of Sharpe (block-diagonal ridge 4.53 -> 4.51 mid, 3.87 -> 3.85 crossed; LightGBM 4.68 -> 4.65; XGBoost 4.64 -> 4.62);
  exits +1 to +4 per model; MaxDD unchanged; no overfull boxes. Consequences not yet handled: the same construction is
  copied in notebook section 11 (`notebooks/_write_0dte_intraday_T_nb.py`, the exit-rule rows of the figure), so the
  figure's model rows now disagree with the table by that amount until the notebook is re-executed; proposal 37's gate
  against the standalone's 2-dp table will fail on re-run (it pins the old 4.53 / 3.87).

## 7. Results that landed after the audit (proposals 40, 41)
- 40 cadence by clock: no schedule beats every-30-minutes; every skip lowers gross Sharpe monotonically; edge controls
  degrade as much as middle skips. Cadence question closed in both directions (with 39).
- 41 OTM-leg ride: in-sample passes (era +0.011/day vs the book, CI excludes 0), OUT OF SAMPLE FAILS on the 413 unseen
  sessions (CI straddles 0; 2025 alone passes, 2024H2 negative). Stress: one-sided, a +5% last-bar jump toward the
  ridden leg costs 11 units (the book 0, hold 10-11 either side). Not adopted. Prior in-sample levels corrected (ask
  0.042 / payoff 0.029 of premium, not 0.064 / 0.050; hedged/flat ordering reversed).
- 41 Part C, the 413 unseen sessions 2024-05-01..2025-12-31 (chain builder reproduces the cache exactly on all 1278
  in-sample days; settlement = GSPC close; mid-inverted IV): the EXIT BOOK earns +0.025 premium units/day (HAC t 1.24,
  Sharpe 1.05) but PER CONTRACT mean -0.02 index points, Sharpe -0.03. Out of sample the exit book is positive only
  under the 5:1 inverse-premium sizing. 2024H2 strong (3.14), 2025 flat-to-negative (-0.037 units, -1.12 points).
  Hold-to-settlement is the only variant with t > 2 (Sharpe 1.75, t 2.11). The 11:00 premium/spot is unchanged
  (0.33-0.42%), but the implied-over-realized remaining-window curve changed SHAPE: ~1.00 flat in sample vs 0.78-0.84
  at 11:00-13:00 in 2025 (morning implied BELOW realized) and 1.16-1.25 after 13:30. The morning premium is gone; the
  afternoon/settlement premium remains.
Implication: the 11:00 exit-at-15:30 book should not be traded as designed; the entry clock and the hold/exit choice
need re-examination on 2024-25 data before any venue or live work; the premium curve by clock and year is the headline
object for the paper.
- 42 forecast-conditional OTM ride (ride the OTM leg iff s_15:30 < 0): 0 of 16 era cells adopted; the signal separates
  payoff days (era) but no cell beats the UNCONDITIONAL ride; reverse controls fail as controls should; forecasts end
  2024-04-30 so untestable OOS, where the unconditional ride already fails. OTM-leg line closed.
- 43 causal entry over time (all 1279 sessions, per contract and premium units, forecast-free): the premium walked from
  the morning to the afternoon 2020 -> 2025 (hold per contract: morning +1.66 -> +0.10 pts/day, afternoon -0.12 -> +0.48);
  implied/realized 10:00 1.17 -> 0.86, 11:00 1.04 -> 0.77, 14:00 0.87 -> 1.10. On the 413 unseen sessions six afternoon
  clocks are positive per contract with t > 2 (13:00 hold t 2.2, 13:30 hold t 3.2, 13:30/14:00/14:30 flatten t 2.1-2.2,
  14:00 hold 2.3); fixed 11:00 t 1.0 hold / 0.0 flatten. Proposal 26's expanding selector sat on 11:00 (845/1279) and
  moved to 10:30 in 2024Q3; 1 of 6 selector cells clears (0.3 expected). Later entries hedge far less (turnover
  12,400 -> 3,700 pts/day). Redesign target: afternoon entry 13:30-14:00; next single cell: a selector on the trailing
  implied/realized ratio by clock (the observable that moved), not on realized Sharpe.
- 44 premium-ratio clock selector (mean of per-session LOG ratios): DEGENERATE by my estimator choice: mean-of-logs is
  monotone in the clock (one-bar remaining windows explode the log), so it picked 15:00 on 1216/1216 sessions; 0/16.
  Process trip: I specified an estimator that cannot see the migration the map (a ratio of sums) showed.
- 45 wings on the 13:30 hold book: 0 of 10 cells worth it. Wings are MORE expensive on the afternoon book (less
  premium against the same strike ladder): unseen per contract 1.0% wings cap 3.9 u for -1.27 Sharpe ($69 of $117/day);
  2.0% caps 8.7 u for -0.24. The futures hedge is unbounded (about $15k/contract on a 5% last-bar jump either way).
- 46 selector on the ratio of sums (E1 cross-clock; E2 per-clock relative to its long-run level): 6 of 7 rules track
  the migration with ~1 quarter lag (modal clock morning -> afternoon 2022-2025); causally chosen window gives +0.84
  pts/day held on the unseen sessions (t 1.9); two cells t > 2 (E2 rolling-126: +1.06 held, +0.73 flattened); none beats
  fixed 11:00 with a CI excluding zero, none beats fixed 13:30 (+1.17, t 3.2, itself read off the unseen sample);
  0 of 14 adopted. Reading: the morning/afternoon split is established; the exact clock is inside per-contract noise;
  the per-clock-relative selector is the defensible causal choice.
- 2026-09-19: `live/ibkr/` rebuilt to the afternoon hold design (verified by me, not by the agents' reports): 161 tests,
  ruff/format/mypy clean on 22 files; extended parity in 35 s asserting (not skipping) the 11:00 book (2.2530773 /
  2.5133605), the 13:30 hold tape on the 413 unseen sessions (+1.173260 pts/day, HAC t 3.210, dev 0), proposal 46's
  selector picks day for day, and proposal 36's V9 series (dev 5e-16); seed ledger `results/live_seed/` (1279 sessions x
  clocks, gated to 43/46/36 at 1e-16..1e-9). Replays: 2023-11-30 fixed 11:00 held n=1 -> -2.5279 units (V9 on; -2.4805
  off = the old package's hold figure); 2025-06-20 selector -> 14:30, stress table -> 3 straddles at $1M capital off a
  $28,812/contract 5%-jump loss, +0.2215 units, flat at close. Runner refuses to size without --capital or --n. All 33
  code-audit items in the runner files and the 12 in the engine files closed with citations (agents' reports
  `runner2_report.md`, `engine2_report.md`). Still untested: every IB socket path (11 guessed ib_async details, ranked in
  README). The selector is a defensible causal choice, not a demonstrated improvement over a fixed afternoon clock.
- 47 (2026-09-19) two-stage rule: gate at 11:00 on a forecast of the 15:30 signal, then the standalone's exit-if-s>0.
  Route A (11:00 forecast x diurnal profile) IS the 11:00 signal by identity (implied side cancels; same sign 865/865);
  route B (causal direct regression) carries some new information and forecasts worse; sign skill at nine bars 0.49-0.62.
  The NON-CAUSAL oracle (gate on the true 15:30 sign) has a lower mean per day than always-hold on every forecast; 0 of
  16 cells adopted; walk-forward picks always-hold on 96-99% of days. Closes the "gate at 11:00 on the afternoon" line at
  the ceiling. Multi-horizon per-bar forecasts do not exist on disk (route C empty).
- 48 (2026-09-19) second-order delta refinements (per-leg vol, vendor delta, skew-adjusted, charm, combinations) vs the
  bias-corrected delta, on the 11:00 book (in sample) and the 13:30 hold book incl. the 413 UNSEEN sessions (forecast-free,
  so a genuine OOS test): 0 of 20 gate reads; per-leg, vendor and skew deltas are WORSE on the 11:00 era book (CIs exclude 0
  on the negative side, -0.18 to -0.30); the in-sample-chosen combination (bias + skew + mid-interval charm) is +0.24 on the
  unseen 13:30 book with a CI straddling 0 (t 1.1): the only refinement worth revisiting with more data. The vendor's
  delta column equals the per-leg Black-76 delta at float32. The last-bar hedge lean is a momentum bet with an unstable
  sign; excluded. Hedge stays: package delta from the mid-inverted vol with the per-clock bias correction.
- 49 (2026-09-19) full-history catastrophe replay, 6,593 sessions 1998-2024 through the 13:30 hold book at frozen
  median premium (11.65 pts): single-session tail p1 -63 pts, p0.5 -81 (7.0 u, $8.1k), p0.1 -164 ($16.4k), worst -432
  (2008-10-10); worst-1% loss share 58% settlement print / 31% whipsaw / 11% earlier bar; hedge leg alone to -$27k. The
  stress table's 5% jump ($30.6k) is beyond the replay's 1-in-1000 -> conservative at the session level. The frozen
  convention is a BOUND, not a model: replay mean -2 pts/day vs the realized book's +0.8 on the same 2020-24 sessions
  (premium co-moves with vol; frozen at 11.65 vs realized mean 14); 2008-09 replays at -16/day. Worst 20-session run
  -$197k/contract = 1.2x the capital at which the 1-in-200 session is 5%: clustering, not the day, is the ruin risk.
  Next: repriced-premium replay (the realistic distribution) and a deleveraging rule on trailing drawdown / vol regime.
  Housekeeping: `proposals/49/a_distribution.csv` is 24 MB; gzip or exclude before any commit.
- 50 (2026-09-19) repriced-premium catastrophe replay (premium and implied fitted to the trailing-21 RV regime on the
  866 overlap sessions, R^2 0.54/0.55; applied to 6,593 sessions): median 13:30 hold per contract mean -0.54 pts/day,
  p1 -45, p0.5 -62, p0.1 -130, worst -360 (2008-10-10); 2008-09 era mean -4.4 (frozen -16). On the overlap the repriced
  replay is still -0.59/day and sd 16 vs the realized +0.80/sd 7.5: both replays are upper bounds on the tail; scaling
  by the overlap's realized/replay p1 ratio (~0.5) puts a normal-premium book's 1-in-200 near $3-6k and 1-in-1000 near
  $6.5-13k per contract; the stress table's $30.6k is 5-10x beyond. Worst 20-session run ~1.0x the capital at which a
  1-in-200 session is 5%: clustering is the ruin risk. Deleveraging candidate (halve above the expanding lagged 90th pct
  of RV21, zero above 97.5th; 93% of sessions full) cuts the worst-20 run to 0.32x capital, sd -27%, mean improved. Design
  addition: adopt a volatility-regime deleveraging rule (the insurer's "do not write into the season"). Blind spots:
  intra-bar path (bar net move explains ~1/3 of squared minute moves on the worst bars), 2008 RV 1.23x beyond the
  calibration's support.
- 2026-09-19: deleveraging rule (proposal 50's candidate) wired into `live/ibkr/` behind `--no-delever`: multiplier
  1 / 0.5 / 0 on the trailing-21-session realized variance vs its expanding lagged 90th / 97.5th percentiles, 252-session
  warm-up, applied to the stress-table size, journaled as a `regime` record, preflight refusal at zero. Gated bit-identical
  to 50's helper on the 1279-session ledger (1223 full / 38 half / 18 zero; first half 2022-02-22; all zeros in April
  2025; March 2020 is inside the warm-up on this ledger, so the rule would NOT have stopped the COVID crash here).
  Verified by me: 177 tests, lint clean; 2025-04-09 replay ends FLAT with the reason (with --no-delever: 6 straddles,
  -0.36 units); 2025-06-20 unchanged (14:30, 3 straddles, +0.2215). The session-RV proxy is the ledger's 10:00 remaining
  variance from 30-min spot steps, not the panel's 1-min 09:30-16:00 sum (corr 0.92, ratio 0.74); stated in README 1.5.
- 2026-09-20: does the 15:30 sign(s) trade decay by year? Read off the deck's day books (no new study). Ridge, per
  premium: mid Sharpe 1.36 / 0.95 / 0.11 / 2.18 / 2.09 for 2020..2024 (n 158/158/219/248/83), crossed 0.56 / 0.44 /
  -0.33 / 1.86 / 1.80; time-trend HAC t +0.8 mid, +1.1 crossed; second half minus first half +0.75 crossed, CI
  [-1.31, +3.10]. No decay; no year's crossed interval excludes zero. What moved is the composition: always short at
  15:30 went -0.43 / -0.48 / +1.94 / +0.88 / -2.06 and the rule's buy share 47 / 25 / 25 / 51 / 61 %; by 2023-24 the
  trade earns on the BUY side. Per contract at the quotes the ridge's whole-sample mean is -0.03 pts/day (2020 alone
  -1.31); xgb/lgbm +0.28/+0.30. The forecasts stop 2024-04-30, so there is no unseen year for this trade.
- 52 (2026-09-20) buying the held legs back at 15:00 on the one-bar-stale, VRP-scaled signal
  (`52_flatten_1500_vrp.py`; tape = 43's, gated exact at X = 15:30; held strikes' own quoted asks). Forecast-free leg,
  all 1278/1279 sessions: flatten at 15:00 minus hold = -1.04 pts/contract/day on the 11:00 book (HAC t -7.6; unseen
  -0.89, t -3.7; -0.69 even at the held legs' MIDPOINT) and -0.87 on the 13:30 book (t -4.7); negative in every year
  2020-2025. The last hour carries 73% of the 11:00 hold book's in-sample points (1.53 hold vs 0.41 flat at 15:00) and
  all of the 13:30 book's. Rules: 192 cells (8 forecasts x stale/fresh x sign/scaled/margin x 2 entries x 2 flatten
  clocks), 0 pass, 0 reverse passes (4.8 expected by chance on the bootstrap leg). Headline cells (ridge, stale, scaled,
  15:00): -0.17 pts/day vs hold on both books, intervals wholly negative, placebo p 0.65 / 0.82. The signal does rank
  days (Spearman of s/median with the gain +0.03..+0.16; the "cheap" tercile loses 0.8 pts at the ask against 1.2-1.5
  in the "rich" one) but no tercile is positive at 15:00 even at the mid. Reading of the old vintage: the stale sign
  agrees with the fresh one on 65% of days at 15:30 (buy share 9.5% vs 40%), so magnitude weighting was filtering a
  noisy sign, not locating cheap options. Verdict: never flatten at 15:00 on these signals; in-sample for the rules.
- 53 (2026-09-20) regressing by time of day (`53_per_clock_regression.py`, outputs under
  `results/atm_straddle_0dte_1530/proposals/53/`; realized variance gated equal to the production panel's, ridge 15:30
  QLIKE 0.1100 and sign(s) 1.338322 reproduced). Every production forecast pools ONE coefficient vector over all 48
  bars; time of day enters only through the diurnal baseline and a session-pooled recalibration. (A) First stage, a
  log-linear HAR proxy on regular-hours bars + overnight, daily causal refits, 6,190 sessions: a separate regression per
  clock beats the pooled one, QLIKE 0.1540 -> 0.1483 (expanding, per-clock smear; interval [-0.0071, -0.0043]); the gain
  sits at the edges of the session and the 14:00 announcement bar (10:00 -8%, 14:00 -6%, 14:30 -5%, 15:00 -8%, 15:30
  -14%, deck period 15:30 -21%), nothing 10:30-13:30. A clock intercept alone gets a third. On the production's
  250-session window plain per-clock is NOT significant (250 rows a clock); shrinking per-clock coefficients toward the
  pooled vector with a causal penalty is (-0.0038, t -8.5). (B) Second stage on the production forecasts: a per-clock
  Mincer-Zarnowitz map makes all four forecasts WORSE (ridge +0.0015..+0.0035, intervals above zero); the structure is in
  the lag weights, not the level, so it cannot be bolted on afterwards. (D) Unplanned and larger: the proxy carries
  information the ridge lacks. The parameter-free geometric average of the ridge with even the POOLED proxy takes the
  12-clock QLIKE 0.1818 -> 0.1672-0.1682, with per-clock proxies 0.1648-0.1674, improving at 10-11 of 12 clocks;
  encompassing t of the proxy 4.9-17 at every clock; at 14:30 the ridge's coefficient falls to 0.03-0.11. At 15:30 the
  ridge alone (0.1100) beats every proxy (0.110-0.146) but the average is 0.0986-0.1020. Trade inputs at 15:30 (mid,
  864 days): ridge alone 1.36; averaged with a per-clock proxy 1.34-1.62 (hit 0.64-0.70); averaged with the POOLED proxy
  0.68-0.73 despite the best hit rate (0.71) because it buys only 25% of days: hit rate is not the trade's metric either.
  All in-sample for the averages' trade rows; the first-stage comparison is out of sample by construction. Next: clock-
  interacted persistence block in the block ridge with shrinkage to the pooled block (cluster run), and a three-way
  causal combination ridge + implied slice + per-clock proxy scored through 51's ladder.
- 2026-09-20/21: subsection regressions in the PRODUCTION linear pipeline, run on Hoffman2 by hand (no hpc-agent; SGE
  arrays, canary 14829448 -> fleet 14829449/50 -> pooled 14829451 -> score 14829452; deployment
  `/u/scratch/j/jamesdc1/harxhar-subsection`, scripts in `cluster/`, tables pulled to
  `results/linear_subsection/baseline_hoffman2/`). `specs/causal_tune_linear.py` with `segment=` + `lag_scope=global`
  (lags and diurnal target on the full 48-bar series, fitted rows sliced), HAR + calendar bucket, ridge / lasso / elastic
  net causally tuned, windows 500 and 2000 days, 13 one-bar arms + 4 blocks + pooled = 108 arms, all finished, canary
  equal to the local run to six decimals (0.119798). Fixes on the way: `slice_to_segment` sent a one-bar range (start ==
  end) down the wrap-around branch and sliced NOTHING; the earlier `results/linear_no_on` runs used `lag_scope=intra`
  (the 16:00 bar's nearest lag was yesterday's close, QLIKE 0.21). Same window, same bars, 2012-2024: own coefficients
  beat pooled at every bar but 10:00; mean QLIKE change ridge -3.9%, lasso -5.1%, elastic net -5.3% (500 days), -5.8 /
  -6.7 / -6.7% (2000 days); 30 and 32 of 39 cells with a day-block interval below zero, none above. Largest: bar ending
  14:30 -18..-22%, bar ending 16:00 (the 15:30 forecast) -6..-8% at 500 days and -12..-13% at 2000 (2020-24: 0.1202 ->
  0.1046), 15:00 -7..-9%, midday -3..-10%; the open (10:00, 10:30) nothing. Blocks: close -5..-8%, core -4..-6%,
  release -6..-13%, open nothing. The best window differs by arm: pooled prefers 500 days, one-bar arms 2000, so
  one-bar-2000 against the deployed pooled-500 is mixed across bars (11 of 39 better, 3 worse) but -11..-12% at the 16:00
  bar and -20% at 14:30. The 15:30 sign(s) trade on the 866 deck days, mid / crossed: pooled 1.05 / 0.58 (500) and 1.14 /
  0.67 (2000); one-bar 1.01 / 0.54 (500) and 1.36-1.46 / 0.89-0.99 (2000); the deck's block ridge 1.34 / 0.87. A
  HAR + calendar model with its own 15:30 coefficients matches the block ridge on the trade and is at least as accurate
  at that bar (0.1046 on 1,087 sessions vs 0.1100 on the 866 deck days; different day sets). Twelve trade cells, one
  sample: a 0.3 Sharpe gap is inside the noise. These forecasts are the spec's own raw-space reconstruction, not the
  deck's recalibrated rv_hat. Row-unit constants apply to the slice (3125-row burn-in, 125-row validation tail, tuning
  every 250 solves), so one-bar OOS starts ~2012 / ~2018. Next: the eight wider feature buckets (minutes per arm now),
  and per-clock coefficients inside the block ridge.
- 2026-09-21: per-clock HAR deltas INSIDE the block ridge, Hoffman2 (canary 14830291, fleet 14830293, score 14830294;
  tables in `results/unification_clock_hoffman2/`). Arms `blk2_clockhar_a{1,100,1000}` = blk2_user + a block of, for each
  of the 13 regular-hours bar-end clocks, the clock dummy and dummy x every HAR rung (169 columns, 1,313 in all), at the
  ladder's own three penalty levels; incumbent panel (the FOMC panel's cache lives on CARC only); scored through the
  deck's recalibration. GATE: the rerun pooled arm equals the published `yhat_blk2.parquet` to 2.7e-12 on all 276,317
  rows. Regular-hours QLIKE vs pooled: penalty 1 +0.7% (significantly WORSE at six bars: a free per-clock ladder on a
  24,000-bar window is too loose), penalty 100 -1.0%, penalty 1000 -1.1%, both with intervals below zero; 2020-24:
  -1.1 / -2.0 / -1.2%. The gain sits at two bars: the bar ending 15:00 (-3.4..-6.9%, 2020-24 -4.5..-13.8%) and the bar
  ending 16:00 (-3.2..-4.8%; 2020-24 -0.2..-2.1%); elsewhere within +-1%. Penalty 1000 is never significantly worse
  and improves seven bars. The 15:30 sign(s) trade does not move: pooled 1.27 / 0.80, clock arms 1.22-1.32 / 0.76-0.86
  (incumbent panel, hence 1.27 not the deck's 1.34). Reading: the block ridge's 1,144 columns (session-edge
  interactions, products, the wide exog basis) already carry most of what own-coefficients-per-bar buys the HAR +
  calendar pipeline (-4..-13% there, -1..-5% here).
- 54 (2026-09-21) the last half hour on the last trading day of the month (`54_month_end_close.py`, outputs
  `results/atm_straddle_0dte_1530/proposals/54/`; gates: ridge sign(s) 1.338322 / 0.869588, chain-built 15:30 straddle
  return equal to the deck's to 2e-7). FOUND BY INSPECTION, so read Part A as the suggestion and B-D as the attempt to
  break it. (A) The 15:30 trade is a tail trade: the long straddle is positive on 38% of days (median -0.33, 4.2x
  premium on its 20 best days); 20 of 866 days carry 73 of the trade's 82 premium units; the ridge was long on 11 of
  those 20 (best forecast 14); on the 262 days the eight forecasts disagree the ridge earns nothing (0.11 mid / -0.16
  crossed) and trading only on the 604 agreement days gives 1.59 / 1.20 (difference +0.33 crossed, interval [-0.32,
  +1.00]). The ridge is long on 17% of month-ends against 40% of all days. (B) The underlying alone, 1998-2019, 264
  month-ends, before any option in the study: last-bar variance over the prior bar's is 1.53 on month-ends against 1.11
  (t 7.9), median squared net move over prior-bar variance 1.14 against 0.53; 2020-24: 2.16 vs 1.55 (t 3.5), 2.15 vs
  0.71. (C) Forecast-free, long the 15:30 straddle on month-ends, at the quoted ask: deck period +0.43 premium (n 52,
  t 2.5), HOLDOUT 2024-05..2025-12 +0.43 (n 18, t 1.2; same mean), all 70 +0.43 (t 2.7), +2.9 index points per
  contract; other days -0.05 at the mid. (D) sign(s) with the ridge overridden to long on month-ends 2.00 / 1.54, to
  flat 1.67 / 1.22 (in-sample). The short afternoon hold books LOSE on month-ends: sold 11:00 -1.8 pts per contract
  (other days +1.4, t 5.7), sold 13:30 -1.8 (other +1.1, t 4.7), sold 14:30 -2.3 (other +0.8); bought back at 15:30
  they lose only -0.6, which places the loss in the last half hour. Neither the forecasts (no month-end feature) nor
  the option market prices the month-end close. Open: quarter-end, index-rebalance and triple-witching days as a
  pre-registered family; a month-end guard in `live/ibkr`; a calendar block in the forecasts.
- 2026-09-21: subsection regressions, wave 2 (nine wider feature buckets, 972 arms, Hoffman2 jobs 14830109-12, all
  finished; tables in `results/linear_subsection/wave2_hoffman2/`). The recursive lasso blew up in a few cells (the
  known lam2=0 singularity): the POOLED lasso at 500 days on `sentiment` and `vol_demand` (QLIKE 20-24 on every bar,
  so their "-35%" is an artefact) and the one-bar lasso on `implied_vol` (2 bars) and `sentiment` (1); those cells are
  excluded and the ridge is the clean read. Ridge, own coefficients per bar against pooled, same window, 13 bars:
  with 2000 days every single-source bucket improves (moments -1.5%, liquidity -3.0%, vol_demand -4.5%, fomc -4.1%,
  market_ew -4.7%, market_vw -4.9%, implied_vol -6.0%, sentiment -6.1%; 3-11 bars with intervals below zero, one bar
  worse in one bucket); with 500 days the gain fades as columns are added (baseline -3.9%, fomc -3.5%, sentiment -2.7%,
  others under 1%, moments +2.2% and liquidity +1.7% with 3-5 bars worse). `all_features` (47 sources): one-bar arms
  LOSE at 500 days (+4.1%, 8 bars worse) and are flat at 2000 (+0.5%). Same lesson as the block ridge: a clock's own
  coefficients need rows in proportion to its columns. Best forecast of the 15:30-16:00 bar in 2020-24: `implied_vol`
  (VIX, VVIX, VIX3M) with its own 15:30 coefficients and 2000 days, QLIKE 0.0992 against 0.1193 pooled. 15:30 sign(s)
  mid / crossed, averaged over 10 buckets x 3 estimators: pooled 0.91 / 0.44 (500) and 1.22 / 0.74 (2000); own 15:30
  coefficients 1.05 / 0.57 and 1.47 / 1.00; single cells range 0.4-1.9 and are not to be picked among.
- 57 (2026-09-21) the forecast-agreement filter on 15:30 sign(s) (`57_forecast_agreement_filter.py`, proposals/57; gates
  1.338322 / 0.869588 and study 54's 604 agreement days, 1.589 / 1.199; every forecast confirmed issued at 15:30). Three
  sets fixed in advance (all eight; six without the near-duplicates `blk2_inc` and `lasso_t`; four families), four
  parameter-free rules, crossed series, paired block bootstrap and a run-length-preserving placebo: 0 of 12 cells pass
  (0.3 expected). Every point estimate is positive (+0.04..+0.34 crossed) but no interval excludes zero, and the
  headline +0.33 for "trade only when all eight agree" falls to +0.19 / +0.20 once the near-duplicates go. The filter
  works by abstaining (flat on 11 of the 20 best payoff days, drawdown -22 -> -13), not by catching more of the tail;
  it is only partly a proxy for a small signal (overlap with an equal-size no-trade band 43-60%). With month-ends
  forced long the ridge is 2.00 / 1.54 (difference +0.67, interval [+0.15, +1.21]); agreement on top adds +0.24..+0.43
  with intervals through zero. NOT ADOPTED as an alpha layer; usable as a risk filter; retest when forecasts exist past
  2024-04.
- 55 (2026-09-21) calendar overrides on 15:30 sign(s): month-end (found by inspection, study 54) and the FOMC statement
  day (proposed a priori) (`55_calendar_overrides_1530.py`, proposals/55; gates 1.338322 / 0.869588, chain return equal
  to the deck's to 2e-7, study 54's 52 / 18 / 70 month-ends and +0.4301 reproduced; the repo's FOMC flag agrees with the
  published schedule on all 30 overlapping days, extended with the 17 published days to 2025-12-10; 46 FOMC days on the
  tape, 32 deck, 14 holdout). Forecast-free, long the 15:30 straddle at the ask: FOMC days +0.10 premium on the deck
  (t 0.6), +0.30 on the holdout (t 0.7), sign unstable by year; month-ends +0.43 / +0.43. The underlying's FOMC close IS
  large (last bar 1.6-1.9x its own 63-session median, t 8.5 in 1998-2019) but the option market largely prices it;
  month-end it does not. Overrides on the ridge, crossed: month-ends forced long +0.67 [+0.15, +1.21], placebo p 0.000 /
  0.001, passes on 8 of 8 forecasts; forced flat +0.35, 4 of 8; FOMC forced long +0.13 [-0.16, +0.48], p 0.10, 0 of 8;
  both forced long 2.13 / 1.67, 8 of 8, the pass carried by month-end. Short hold books on FOMC days: noisier, not
  reliably losing (sold 13:30 +0.53 vs +0.93 other days; only the 14:30 entry, sold into the press conference, is
  negative, -2.4, t -1.1). Verdict: month-end override supported; FOMC force-long harmless but not established.
- 56 (2026-09-21) the close-calendar family, tail capture, and a causal calendar term in the forecasts
  (`56_close_calendar_family.py`, proposals/56; five gates incl. 1.338322 / 0.869588 and the panel's 16:00-row rv_hat
  equal to the deck's). (A) Eleven day types judged by a rule written first (at-the-ask long mean positive with t > 2 on
  the deck, same sign on the holdout, present in the 1998-2019 underlying): only month-end and its MSCI-review subset
  survive (MSCI months +0.67 deck / +1.14 holdout, n 17 / 5; month-ends without them +0.31 / +0.16), and both had been
  seen; quarter-end, first session, pre-holiday, CPI, employment, Russell: no. NEW LEAD, observation only, on the SHORT
  side: third Fridays — long at the ask -0.31 (t -3.2) deck, -0.57 (t -4.2) holdout; short at the bid +0.25 (t 2.4)
  and +0.55 (t 3.9), +0.33 (t 3.9) over all 72 — while the underlying's close is not quieter those days. Needs its own
  pre-written rule. 2026-09-21 side check (mine): forcing short on the sessions before/after FOMC passes in-sample
  (+0.31 crossed, [+0.10, +0.56], p 0.002) but the holdout excess over ordinary days is zero (+0.07 vs +0.065) and the
  underlying's post-FOMC close is LOUDER (1.21x, t 3.0): not adopted. (B) QLIKE and tail capture rank the forecasts
  almost oppositely: the fixed lasso is last on QLIKE at the close bar (0.111) and first on tail capture (14 of the top
  20, p 0.008); the baseline OLS is mid-table on QLIKE and last on tail capture (7 of 20). (C) A causal expanding-window
  calendar term on log(rv_raw / rv_hat) at the 16:00 bar: month-end gamma +0.364 (Newey-West t 10.2, variance x1.44),
  quarter-end +0.200 on top (t 3.0), others small; QLIKE at that bar improves for all eight forecasts (4-5%, all
  intervals below zero 2003-2024); sign(s) with the adjusted forecast rises for all eight (ridge 1.34 -> 1.56 mid, 0.87
  -> 1.10 crossed; top-20 capture 11 -> 13) with no paired interval excluding zero. The forecast-level term recovers
  about a third of what the rule-level month-end override does (+0.23 vs +0.67).
- 2026-09-21: month-end sit-out guard in `live/ibkr/` (new `calendar_guard.py`: NYSE session calendar from the
  exchange's own rules — holiday observance, Good Friday via dateutil.easter, recorded special closures — so it works
  for future dates with no new dependency; `is_last_session_of_month`; a registry of named no-short calendars with only
  `month_end` enabled; ON by default, `--no-calendar-guard` to override; refusal in preflight with the reason; a
  `calendar` journal record every session; `"calendar"` and `"regime"` added to `journal.EVENT_KINDS`, closing the
  2026-09-19 audit item). Parity: on the seed ledger's 1279 sessions the rule flags exactly study 54's 70 month-ends
  (the two missing of 72 months, 2024-11-29 and 2025-11-28, are half sessions the ledger never carried); against the
  1998-2024 panel no rule-holiday has a 16:00 bar. Verified by me: 207 tests pass (177 before), ruff clean on 24 files.
  Replays: 2023-11-30 ends FLAT in preflight with the month-end reason; with `--no-calendar-guard` it sells 4 straddles
  at 14:30 and loses 4.73 premium units (-$10,779 at $1M); 2025-06-20 unchanged (14:30, 3 straddles, +0.2215). Note
  the package's standard "worst day" replay date IS a month-end, so its shared test helper now defaults the guard off.
  Still untested: every IBBroker socket path; short-notice special closures must be added by hand; `previous_trading_day`
  in run_day.py still ignores holidays (pre-existing).
- 58 (2026-09-21) selling the 15:30 straddle on the third Friday (`58_third_friday_short.py`, proposals/58; gates incl.
  study 56's 52 / 20 third-Friday rows). NOT a clean out-of-sample test: study 56 had printed both samples. Four criteria
  written before running, all met: (1) holdout short at the bid +0.552 premium, n 20, t 3.93, one-sided p 0.00045, x11
  day types = 0.0049; (2) third Fridays beat the other Fridays of the month in the deck period (+0.26) and the holdout
  (+0.40), all sessions +0.30 [+0.07, +0.52]; the third Thursday and third Wednesday show nothing, so it is the
  expiration Friday and not the week; (3) random-day placebo p 0.003; (4) positive in 5 of 6 years (2020 -0.08, then
  +0.26, +0.32, +0.43, +0.64, +0.43). All 72: +0.334 premium (t 3.9), hit 69%, +1.90 index points per contract at the
  bid on a 7.6-point premium, worst three days only -1.7, -1.6, -1.6 premium units (every other session's worst is
  -11). The underlying's close is NOT calmer (2020-24 last-bar variance 1.25x its own median vs 1.02x on other Fridays;
  squared net move the same), so the market overprices the expiration-Friday close. For 15:30 sign(s) nothing changes:
  the ridge is already short on 81% of third Fridays (forced short +0.01, p 0.35). For the short afternoon hold books
  third Fridays are the best days: sold 13:30 +3.00 points per contract (t 5.3, n 72) against +1.06 on other Fridays
  and +0.80 on every other session; sold 11:00 +2.98 (t 3.4). Side observation, not tested: the FIRST Friday is +0.51
  (t 4.2) on the holdout and +0.13 on the deck. Next genuinely new evidence: 2026 third Fridays.
- 59 (2026-09-21) a loss aligned with the 15:30 sign(s) trade (`59_trade_aligned_loss.py`, proposals/59; gates
  1.338322 / 0.869588, ridge QLIKE 0.1100, QLIKE equal to the 1/c^2 mixture of elementary scores day by day to 3.8e-7,
  and mean(qR) = mean|R| - 2 mean(economic loss) exactly). The trade's P&L is |R|-weighted sign accuracy (the economic
  loss); for a variance forecast the consistent counterpart is the elementary score of the mean at the day's implied
  slice, ES = |y/slice - 1| on days forecast and outcome sit on opposite sides of it, and QLIKE = integral of ES_c
  c^-2 dc while the trade uses only c = 1. A PERFECT variance classifier agrees with the payoff's sign on only 62.9%
  of days (4.53 mid / 4.07 crossed Sharpe against 17.3 for a perfect payoff-sign rule): about a third of the payoff is
  the net move against the strikes, out of reach of any variance forecast. Murphy diagram: the trees are best for
  c in 0.77..1.4 and the ridge variants rank 6th-8th at c = 1; rankings cross within a factor of two of the slice.
  Across the eight forecasts no variance loss ranks them like crossed Sharpe (Spearman QLIKE 0.12, ES 0.41, intervals
  spanning zero); the accuracy ladders are monotone and cannot discriminate between losses. Causal weighted-logistic
  rules (614 days, 9 cells): 0 pass; variance targets push the fitted threshold to 1.4-3.0x the slice and give up the
  long side; the payoff-weighted fit keeps it near 1 (median 1.10). Recommendation: the paper reports ES at the slice
  and the Murphy diagram beside QLIKE, plus the 62.9% bound; the trade is judged by the economic loss; the remaining
  lever is the net-move/pin side (calendar), not variance accuracy.
- 2026-09-21 quick check (script in the job tmp dir, not a study): at 15:30 on sign(s) BUY days, what to do with the
  insurance book's short. Deck days, index points per contract, paired block bootstrap vs holding. Sold 13:30: go flat
  +0.78 vs hold +0.80 (Sharpe 1.82 vs 1.68, [-0.43, +0.75]); buy back 2x the held legs +0.92 (1.72, [-1.16, +1.00]);
  buy back + fresh 15:30 straddle 1.56; keep the short + fresh straddle 1.76. Sold 11:00: go flat 2.76 vs 3.35
  ([-1.10, -0.13], significantly worse: the last half hour still pays +0.74 on buy-signal days, t 3.9); net long 1.62
  ([-2.86, -0.70], worst day -104). The fresh 15:30 straddle bought at the ask on buy-signal days earns +0.04 per
  contract (t 0.1). Verdict: never exit and never double up on the signal; the earlier "use the 15:30 signal as a
  filter on the insurance book" was wrong (study 52 had already tested it). Reading, untested: the signal prices a
  fresh at-the-money straddle; the 13:30 legs are still near the money at 15:30 (the signal splits their last half
  hour, +0.04 vs +0.74), the 11:00 legs usually are not (+0.74 vs +0.87). The one long that pays per contract is the
  month-end straddle (+2.9), which the live override is being built for.
- 2026-09-21: `live/ibkr` month-end OVERRIDE replaces the sit-out guard (user: "I don't want month-end guard, I want
  month-end override"). `month_end_mode` = override (default) / sit_out / off (`--no-calendar-guard` = off). On the last
  session of a month in override mode: no short, the session still quotes and inverts the straddle at every stamp from
  the selector's clock so the premium ledger keeps its rows, and at 15:30 it BUYS the short program's stress-table count
  (without the deleveraging multiplier) of nearest-OTM straddles with a limit at the quoted ask that never crosses
  further (cancelled if unfilled), held unhedged to cash settlement and reconciled at the official close; refusals end
  FLAT with the reason. Parity: `python -m live.ibkr.parity --month-end` trades all 70 month-ends and matches study 54 to
  3.6e-15 points per contract (+2.9306 points, +0.4301 premium units). 220 tests (207 before), ruff/format/mypy clean on
  24 files (verified by me). Replays at $1M: 2023-11-30 override buys 4 straddles at 3.65, reconciled +19.15 points per
  straddle (+5.25 units); sit_out refuses; off sells 4 and loses 4.73 units as before; 2025-06-20 unchanged. Sizing the
  long to the full loss budget instead would be a median 28x as many contracts (4-96x); not enabled. Untested: every
  IBBroker socket path including the new non-crossing BUY; a month-end that is also a half session.
- 60 (2026-09-21) moneyness at 15:30 (`60_moneyness_1530_atm.py`, holdclose/proposals/60; gates: proposal 43's, the 15:30
  straddle equal to the deck's R, the gamma formula equal to a central difference of the delta to 2.5e-5, the quick
  check reproduced). Moneyness g = gamma of the held legs / gamma of the 15:30 straddle (median 0.40 for the 11:00 book,
  0.68 for 13:30, 0.91 for 15:00). H1 (the 15:30 signal speaks to the held short only when its legs are near the money):
  NOT SUPPORTED — within an entry clock the signal x moneyness term is t +0.5 (wrong sign) and the near-the-money third
  shows the SMALLEST sell-minus-buy gap (+0.08 [-1.04, +1.10]) while the far third shows +0.35 [+0.15, +0.53]. What
  varies is the entry clock itself: the gap rises from about 0 for a 10:00-11:30 book to +0.5..+0.8 for 12:30-15:00;
  no tested reason. H2 (the at-the-money straddle is the better instrument than the held legs): SUPPORTED — buying g
  15:30 straddles on buy days beats buying the held legs back by +0.52 Sharpe at 11:00 [+0.28, +0.80] and ties at 13:30
  (+0.09 [-0.15, +0.34]); the earlier buy-back experiments used the worse instrument. H3 (any signal overlay beats simply
  holding): NOT SUPPORTED — 11:00 -0.07 [-0.43, +0.29], 13:30 +0.23 [-0.29, +0.76]; buying the same gamma on SELL days
  (control) does as well at the morning clocks, better only after 13:00. The month-end override on the book again:
  11:00 all sessions 2.23 -> 2.67, holdout 0.77 -> 1.31 (month-end days -5.6 -> +3.1 points); 13:30 1.81 -> 2.26,
  holdout 2.06 -> 2.55. Verdict: hold the insurance book, change only the month-end.
- 2026-09-21 CORRECTION to waves 1-2: the fair re-score (`experiments/score_linear_subsection_causal.py`, Hoffman2 job
  14840712, tables in `results/linear_subsection/causal_rescore_hoffman2/`). The spec's raw-space back-transform uses one
  look-ahead, whole-run second-moment term per arm, so a one-bar arm got a clock-specific calibration the pooled arm
  never had. Rebuilt with a causal trailing term (250 sessions, lagged) and split on identical bars, all estimators,
  blown pre-fix lasso cells excluded (145 of 6240). Whole sample, mean QLIKE change: as scored -2.6% (500 days) /
  -5.0% (2000); COEFFICIENTS only (same clock calibration on both sides) -1.7% (113 better / 47 worse of 375) / -4.3%
  (210 / 12 of 387); CALIBRATION only (the pooled model given a per-clock term) -2.0% / -3.1% (296 / 4 of 390); both
  together against the pooled model as deployed -3.6% / -7.3% (300 / 0). So the per-clock coefficient gain is real and
  is most of the reported number at 2000 days; about a third of it at 500 days was calibration; and a per-clock
  second-moment term is a separate ~2-3% win for this spec's back-transform (not the block ridge's MZ recalibration,
  which study 53 found a per-clock map makes worse). Ridge at the 16:00 bar with 2000 days: coefficients -7..-13%,
  package -13..-18% (implied_vol -17.7%). The 47-source design still loses at 500 days (+4.4%). 15:30 sign(s), mean over
  buckets and estimators, 2000 days: pooled with pooled calibration 1.17 / 0.70, own 15:30 coefficients with clock
  calibration 1.40 / 0.92.
- 61 (2026-09-21) net LONG the 15:30 at-the-money straddle on buy days, on top of the insurance book
  (`61_long_atm_1530.py`, holdclose/proposals/61; study 60's gated tape). H4 (net long adds to the book at 11:00 or
  13:30): NOT SUPPORTED. Net long one straddle (buy g + 1): 11:00 2.80 vs hold 3.35 (-0.55 [-1.57, +0.33]), 13:30 1.64
  vs 1.68 (-0.04 [-1.25, +1.04]). Net long the book's own premium (2.3-3.5 straddles at the median): 11:00 1.66
  (-1.69 [-3.06, -0.51], significantly worse), 13:30 1.28. Worse than hold at every entry clock. The signal does sort
  the days (the same long on SELL days is far worse: 1.93 and 0.66), and the premium-sized long adds +0.23 points per
  short contract on buy days, but one 15:30 straddle bought at the ask earns only +0.04 per contract (t 0.1) on buy
  days, so the long leg brings several times more variance than money. With the month-end override the net-long book
  reaches 2.09 at 13:30 (month-end doing the work).
- 2026-09-21 quick check: combining the insurance book with the 15:30 sign(s) trade. Positions add, so one account and
  two accounts give the same total P&L; what matters is relative size (and, in one account, netting on the 11-16% of
  days the held strikes equal the 15:30 strikes, plus margin offset). Daily correlation +0.07 (11:00 book) / +0.05
  (13:30), negative on buy days (-0.32 / -0.52) and positive on sell days (+0.34 / +0.47). Adding the 15:30 trade raises
  the total's Sharpe iff its Sharpe (0.88 crossed, per premium) exceeds correlation x book Sharpe (0.22 / 0.08): it
  does. In-sample best mix 3.41 (17% of risk in the 15:30 trade) / 1.86 (33%); 10% of risk +0.05 [-0.08, +0.20] /
  +0.08 [-0.05, +0.23]; equal risk 2.89 / 1.76. Study 61's overlays failed on size (the long carried far more risk than
  return per short contract), not on combining.
- 62 (2026-09-21) running the insurance book and the 15:30 sign(s) trade together (`62_combine_book_and_1530.py`,
  holdclose/proposals/62). Positions add (one account = two accounts; netting changes margin only). Both streams scaled
  by trailing sd, weights from past days only (expanding, 252-day warm-up, scored 2021-08-11 .. 2024-04-30, 613 days).
  H5 (Sharpe-proportional causal risk share beats the book alone at 11:00 or 13:30, either layer): 1 of 4 cells, at the
  edge — book with the month-end override + 15:30 trade forced long on month-ends, 11:00: +0.31 [+0.005, +0.68]. Plain
  layer (no month-end anywhere): +0.07 [-0.14, +0.33] and +0.02 [-1.03, +1.04]: no established gain from combining as
  such. The passing layer double-counts the month-end long; with the 15:30 trade kept FLAT on month-ends instead: R1
  +0.22 [-0.03, +0.54] (11:00), +0.44 [-0.34, +1.22] (13:30); a fixed 10% risk share (reported as descriptive, not the
  pre-registered rule) +0.16 [+0.03, +0.33] and +0.22 [+0.02, +0.42]. Causal risk shares chosen: 13-20% (11:00 book),
  about 40% (13:30 book). Reading: a 10-20% risk share for the 15:30 trade beside a month-end-overridden book adds about
  0.15-0.3 Sharpe, established only at the edge and only in-sample (the signal ends 2024-04-30).
- 2026-09-21: the paper now reports decision-aligned scoring for the 15:30 trade (`writeup/sections/results_close_option.tex`,
  new block inside "A Last-Bar Same-Day Option Reading"; generator `writeup/make_murphy_1530_tex.py` -> figure
  `figures/murphy_1530.pdf`, `generated/table_murphy_1530.tex`, 43 macros in `generated/murphy_1530_numbers.tex`;
  Ehm et al. 2016 added to references.bib): the elementary-score mixture identity for QLIKE, the score at the implied
  variance, the Murphy diagram, the 62.9% variance-oracle bound, and the statement that no variance score orders the
  eight forecasts on this trade. One phrase corrected at line 354: the 4.53 ceiling is the realized-variance oracle, not
  a settlement oracle. Build clean, 45 -> 48 pages (build artefacts not committed).
- 63 (2026-09-21) the insurance trade and the 15:30 trade separately vs together, one contract each
  (`63_separate_vs_together.py`, holdclose/proposals/63; gates: the deck's R, study 61's means). P&L: the one-account
  book rebuilt from its own cash flows and strike-by-strike netted settlement equals the sum of the two separate
  accounts to 1.4e-14 points every day (the straddles share both strikes on 11% / 16% of days). Per contract the 15:30
  trade earns +0.001 / -0.005 points a day at the quotes (Sharpe about 0), so the sum carries the insurance trade's
  money with more risk: 11:00 insurance 3.35 -> both 2.09, 13:30 1.68 -> 1.02 (correlation +0.15 / +0.25). What
  differs is capital: the worst loss from 15:30 marks within the live stress band (+-5%) is 19-20% lower together than
  the two accounts' worst losses added (buy days 63-65% lower, sell days unchanged). The 15:30 trade earns its Sharpe
  only per unit of premium (study 62), not per contract.
- 64 (2026-09-21) the insurance book's hedge schedule NET of futures cost (`64_hedge_schedule_net_of_cost.py`,
  holdclose/proposals/64; gates: 43's, H30 equal to 43's hold/flatten/cost series exactly, study 45's 13:30 cost row).
  Seven schedules (every 30 min, hourly, last hour only, a gamma-defined band, half-hedge, entry only, none) x costs
  0 / 0.25 / 0.5 / 1 bp: 0 of 48 cells adopted — nothing beats every-30-minutes with a deck-period interval above zero;
  the holdout ranks the less-hedged schedules higher (the naked straddle happened to earn in 2024-25) at 4-10x worse
  days (naked worst -396 / -160 vs -63 / -40 hedged). COST, not schedule, is the lever: 13:30 book deck Sharpe 1.68 /
  1.33 / 0.98 / 0.29 at 0 / 0.25 / 0.5 / 1 bp; holdout 2.06 / - / 1.21 / 0.38. NET OF 0.5 bp THE 11:00 BOOK IS NEGATIVE
  ON THE HOLDOUT (-0.28); the 13:30 book holds (1.21). Turnover 6,600-9,600 index points a day (13:30). Recommendation:
  keep every-30-minutes, work the hedge fills toward 0.25 bp (passive/mid ES), band as the fallback above 0.5 bp.
- 65 (2026-09-21) capital per contract from history vs the live 5% stress (`65_capital_from_history.py`,
  holdclose/proposals/65; gates: study 50's replay rows and brake counts, the 13:30 book's gross +0.919 and deck net
  +0.4679). Tails as % of the index: replay worst 8.92% (2008-10-10) fell on a brake-ZERO day; worst FULL-size day 3.47%
  (2022-09-21), full-size 1-in-1000 1.87%; realized 2020-25 full-size worst 1.11%. The live stress is 4.93% of the index
  (median). So the stress is 1.4x the worst full-size day in 26 years and 2.6x the full-size 1-in-1000 — NOT 5-10x
  (that earlier comparison used all-day quantiles; retracted). Live book (month-end override), net of 0.5 bp, 10%
  fraction, $1M, 2020-25: stress rule 9.5%/yr, vol 5.8%, Sharpe 1.64, max DD -6.3%, worst day -2.4% (holdout 10.1%/yr,
  Sharpe 1.94); R_worst (1.45x contracts) 13.3%/yr, max DD -8.0%; R_q (2.64x) 24.5%/yr, max DD -13.9%. Replay walk
  (tails only, an upper bound): stress rule worst day -7.1%, worst 20 sessions -20%, no day beyond 10%; R_worst -16.6%
  (2008-09-16), -46%, 4 days beyond 10% — sizing to the worst loss SEEN SO FAR sized up before 2008 and was hit by it;
  R_q -18.8%, -55%, 9 days. The pre-registered criteria pass, but criterion (ii) (against the replay's own run) was too
  lenient to be informative. Verdict: keep the stress table; the most history supports is about 1.45x (R_worst now that
  2008 is in the record); the 1-in-1000 sizing is a bet that days like 2008-09-16 cost about twice the fraction.
- 2026-09-21 lasso rerun after the between-tune identifiability fix (e4e9358), Hoffman2, all 360 recursive-lasso arms to
  `results/linear_subsection_lassofix/` (tables for nine buckets in `results/linear_subsection/lassofix_hoffman2/`;
  all_features' two pooled arms still running, job 14840615 -> scorer 14840624). No cell above QLIKE 1 (before: 20-42);
  the between-tune mask fired in 247 of 358 arms (2,662 times), so the snapshot mask went stale in most lasso arms.
  As scored by the spec (per-arm look-ahead back-transform; the fair causal re-score of these is job 14842753): own
  coefficients per bar vs pooled, 13-bar mean QLIKE change -1.9..-5.1% at 500 days and -3.3..-7.5% at 2000 days, no bar
  significantly worse over 2012-24; the 16:00 bar with 2000 days -7..-16% (implied_vol 0.0995 vs 0.1190 in 2020-24).
  15:30 sign(s) with the lasso forecast, 2000 days: own 15:30 coefficients beat pooled in 8 of 9 buckets (e.g.
  implied_vol 1.64 / 1.16, value-weighted market 1.75 / 1.28 mid / crossed).
- 66 (2026-09-21) Kelly sizing for the insurance book, and the third-Friday multiplier (`66_kelly_sizing.py`,
  holdclose/proposals/66; gates: study 65's realized series and live-book return 9.4863%/yr at 10%, study 58's 72
  third Fridays and their 13:30 gross +2.999 points). r = one full-size contract's day in stress units (a 5% jump = -1;
  live book: 13:30 short net of 0.5 bp, brake, month-end override). Kelly fraction of capital committed to the stress
  day: realized days only 2.51 (live 10% = 0.04 of Kelly — meaningless, the tape has no crash); realized + the pre-2020
  replay tail (24 of 5,505 sessions worse than the realized worst -0.237, worst -0.656 on 2011-08-09 / 2008-09-16; the
  replay's tails are an upper bound) 0.99 -> live = 0.10 Kelly; the same with the mean at its lower 2.5% bound (block
  bootstrap) 0.22 -> live = 0.45 Kelly; a literal 5% jump once in 10 / 25 / 50 years 0.85 / 0.94 / 0.97. By period
  (with tail) deck 0.91, holdout 1.15. Five-year paths (realized days in 21-session blocks + replay tail at 0.436%/day,
  10,000 paths): live 10% median growth 5.8%/yr, P(20% drawdown) 1.7%, P(below start) 2.7%; quarter Kelly (0.25) 13.8%,
  63%, 3.8%; half Kelly (0.49) 25.8%, 99.6% (P(50% DD) 27%); full Kelly 37.9%, P(50% DD) 93%. Verdict: the live 10% is
  half-Kelly on the pessimistic mean with 2008-type days in the distribution, and a tenth of Kelly on the point
  estimate; do not size up to Kelly — the uncertainty in the mean alone moves Kelly 4.5x. THIRD FRIDAYS: mean r 0.0123
  vs 0.0033 on other sessions (sd 0.020 vs 0.037, worst -0.049 vs -0.237, hit 0.75 vs 0.58); Kelly with the tail 1.50 vs
  0.81, ratio 1.86; third-Friday Kelly is pinned by the tail (bootstrap 1.47-1.51, the ruin edge is 1.52), so the
  ratio's uncertainty is the rest of the book's (other-session Kelly 0-1.30, zero in 4.2% of draws). Pre-registered
  rule (lower 2.5% end of the ratio, rounded down to 0.1, floored at 1): x1.1 — adopted as the live default. Realized
  (in-sample; third Fridays were found on these data): x1.1 Sharpe 1.645 -> 1.671, holdout 1.943 -> 1.970; the point
  ratio x1.8 (descriptive) 1.84 / 2.15, +1.4%/yr, and a 5% jump on a third Friday would then cost 18% of capital
  instead of 10%.
- 2026-09-21 live third-Friday sizing (6a66262, `live/ibkr/`): the short count on the monthly-expiration session
  (study 58's rule, 72 of 1,279 sessions flagged, 0 disagreements; 336 on 1998-2025) is multiplied by
  THIRD_FRIDAY_SIZE_MULTIPLIER = 1.1 (study 66) after the brake, floored (decimal arithmetic), capped at
  --max-straddles; never on --n or a month-end override day. 251 tests pass. CAVEAT: at $1M and 10% the table gives
  2-6 straddles and floor(n x 1.1) = n for n <= 9, so x1.1 changes no count below about $2.9M with the cap raised
  (2025-06-20 replay: 3 straddles either way; x2.0 -> 6; $4M with --max-straddles 20: 13 -> 14). Study 66's
  +0.17%/yr assumed fractional contracts.
- 67 (2026-09-21) can the crash be forecast, and what keeps the S&P-plus-insurance book small when it comes
  (`67_crash_forecast.py`, holdclose/proposals/67; gates: study 50's replay and brake, ^GSPC close = the chain's
  settlement close on all 1,279 sessions). A first run built the S&P's daily return from the panel's summed 30-minute
  bars and was withdrawn (correlation 0.98 with the index on matched sessions, misses of 2-3% on the March-2020 limit
  days; an earlier "0.81" came from comparing calendars with different gaps). Signals at the prior close, as causal
  percentiles: RV21, RV5, an expanding HAR forecast, VIX, VIX/VIX3M, depth below the 252-session high. SIZE IS
  FORECASTABLE: 68-89% of the S&P's worst-1% days (<= -3.5%) and 69-94% of the insurance replay's worst-1% days fall in
  the signal's top quintile (15-20% of days; lift 3.7-6.1, every lower bound above 1.8; AUC 0.84-0.94). THE FIRST HIT
  IS HALF FORECASTABLE: first shocks (no crash day in the prior 21 sessions, 21 of them): VIX 64%, HAR 50%, RV5 43% in
  the top quintile, RV21 (the live brake's input) 19% (lift 1.4, not established). 4 of the 10 worst 20-session runs
  began with every volatility signal below its median (2020-02-24, 2001-08-20, 2011-07-25, 2001-02-16); by the eve of
  the worst day inside the 2020 and 2008 runs RV21 and VIX were at 0.99. SIGN IS NOT: in the top quintile the largest
  2% of moves are 49-56% down (65% for VIX/VIX3M), and the mean return is highest there (RV21: 18%/yr vs 3%/yr in the
  bottom quintile). Brake by signal on the replay (pre-registered: lower 1-in-200 day AND worst 20-session run in both
  halves, no more premium given up): RV5 REPLACES RV21 (worst-20 -14.9% vs -19.5%, 1-in-200 -2.06% vs -2.28%, premium
  given up 12.7% vs 12.9%); HAR and VIX/VIX3M cut tails more but give up more premium; VIX fails a half; drawdown depth
  fails. Realized check (added after, not a test): RV5 brake deck Sharpe 1.53 -> 1.77, max DD -6.3% -> -4.5%; HOLDOUT
  1.94 -> 1.67 (10.1% -> 7.9%/yr, same drawdown; 27 braked sessions against 24, no crash to dodge). Live brake NOT
  changed. S&P leg 1998-2024, price only, cash at zero (pre-registered: smaller max DD and worst-20 in both halves,
  Sharpe interval not below zero): volatility-managed (weight min(1, expanding median HAR vol / today's)) growth 6.0%
  vs 6.5%, max DD -49% vs -57%, worst-20 -15% vs -31%, Sharpe 0.48 vs 0.42 (diff [-0.07, +0.19]); 200-session trend
  4.7%, -28%, -17%; the RV21 brake on the S&P 6.4%, -57%, -17%; all three MITIGATE; VIX/VIX3M > 1 to cash fails.
  Inside the dated drawdowns (hold / vol-managed / trend / brake): covid -34 / -16 / -17 / -13%; 2007-09 -57 / -38 /
  -9 / -33%; 2000-02 -49 / -48 / -21 / -53% (volatility rules do not help a slow bear). JOINT book on the tape
  2020-01..2024-04 (S&P leg + the realized live book at 10%): hold 19.0%/yr, Sharpe 0.87, max DD -33%, worst-20 -31%;
  vol-managed 16.6%, 1.09, -19%, -14%; braked 19.3%, 1.06, -23%, -13%; trend 14.5%, 1.04, -20%, -17%. The 1998-2024
  joint rows on the replay (max DD -73..-82%) are NOT a level: where both exist the replayed leg shows -17.7% (covid)
  and -14.0% (2022) against the realized leg's +0.8% and +9.9% (max DD -21.6% vs -6.2%); the insurance leg's true 2008
  is unknown, the replay's -33% is an upper bound. Not covered by any signal: the first gap out of calm (stress sizing
  covers the insurance leg; the S&P leg needs less than full exposure or longer-dated puts, untestable on a 0DTE chain).
- 2026-09-21 fair (causal back-transform) re-score of the fixed recursive lasso, Hoffman2 job 14842753, nine buckets
  (`results/linear_subsection/lassofix_hoffman2/*/subsection_vs_pooled_causal.csv`, `trade_1530_causal.csv`;
  all_features not yet: its two pooled arms are still running, its re-score has to be re-run after them). 1,872 cells,
  none blown. Own coefficients per bar against pooled, both with the same causal per-clock back-transform
  ("coefficients"; % of pooled QLIKE, negative = own better), mean over 9 buckets x 13 bars: -2.9% at 500 days (43 cells
  significantly better, 5 worse of 117), -5.0% at 2000 days (74 better, 2 worse); deck period 2020-24 -1.8% / -5.0%.
  Calibration alone (pooled coefficients, per-clock vs pooled back-transform) -2.0% / -3.1%; the package -4.9% / -8.1%.
  So for the lasso, as for ridge and elastic net, about 40% of the as-first-scored per-bar gain at 500 days was
  calibration and the coefficient gain survives. By bar (2000 days): 14:30 -19.9%, 16:00 -9.1%, 15:00 -8.4%, 13:30
  -4.8%, 15:30 -3.7%; 11:00 +1.0% (the one bar where pooled is not worse). By bucket (2000 days) -2.5% (moments) to
  -6.7% (sentiment). 15:30 sign(s) with the lasso forecast, causal per-clock back-transform, own 16:00-bar
  coefficients against pooled: 2000 days own higher in 7 of 9 buckets (mean Sharpe 1.47 / 1.00 mid / crossed against
  1.14 / 0.71; liquidity and moments the exceptions), 500 days 6 of 9; differences in trading Sharpe between forecasts
  are not tested and are noisy (ridge on the same days 1.34 / 0.87).
- 2026-09-21 live defaults changed on the operator's instruction (`live/ibkr/config.py`, README 1.2 / 1.7, one new
  test; 252 tests, lint, types and `live.ibkr.parity` clean): (i) THIRD_FRIDAY_SIZE_MULTIPLIER 1.1 -> 1.5 — the
  operator's choice between study 66's pre-registered 1.1 (which floored changes no count below ten straddles) and its
  point ratio 1.86; NOT the pre-registered number; (ii) entry_mode "selector" -> "fixed" at 13:30 (every costed number
  in studies 64-67 is the fixed 13:30 entry; proposal 46 adopted the selector in 0 of 14 cells); `--entry-mode selector`
  and `--third-friday-multiplier 1.1` restore the old behaviour. Study 66 part D (added after the choice, descriptive:
  whole contracts on $1M in the runner's order of operations, cap 10, net of 0.5 bp, brake, month-end override,
  2020-25): x1.0 8.32%/yr, Sharpe 1.60, max DD -6.0% (whole contracts cost about 1.2 points a year against study 65's
  fractional 9.49%); x1.1 identical to x1.0 (0 of 72 third Fridays change); x1.5 9.00%/yr, Sharpe 1.71, max DD -5.9%,
  69 of 72 third Fridays sell more (5.6 contracts on average against 3.9), the 5% jump on a third Friday costs up to
  14.9% of capital (holdout 8.91%/yr, Sharpe 2.00 against 8.46% / 1.91); x1.8 9.39%/yr, 1.77, up to 17.9%. In-sample:
  the day type was found on these sessions. Replay 2025-06-20 at the new defaults: 13:30 entry, 3 -> 4 straddles,
  12.0% of capital in the stress jump, +$2,645 (x1.0: 3, +$1,948).
- 68 (2026-09-21) the insurance brake on the S&P leg at the timing the runner can deliver
  (`68_sp_leg_brake_timing.py`, holdclose/proposals/68; gate: L0 reproduces study 67's S&P-leg rows to 1e-9). Study
  67 put the weight for session t on from the close of t-1; the runner learns t-1's variance when the operator
  reconciles it on the morning of t, so the S&P leg is resized DURING t. Bracketed: L0 (from the prior close) and L1
  (a full session late). Pre-registered: a rule is adopted for the runner if at L1 it still meets 67's criterion. The
  BRAKE a session late: worst-20 -16.9% (same as L0) but max DD -57.0% vs buy-and-hold -56.8% in 1998-2011 (a slow
  bear at moderate variance) -> FAILS; the user's instruction "brake on the S&P" is therefore NOT implemented as the
  default. VOL-MANAGED (min(1, expanding median HAR vol / today's)) passes at L0 and L1 (L1: growth 6.0% vs 6.5%, max
  DD -49% vs -57%, worst-20 -14.5% vs -30.9%, Sharpe 0.47 vs 0.42, diff [-0.08, +0.18]); MA200 passes too (L1 max DD
  -23%, worst-20 -14%, growth 5.7%) but was not a candidate for the runner (it needs the index's own 200-day history,
  which the ledger does not carry). Joint book on the tape 2020-01..2024-04: hold 19.0%/yr, DD -33%, worst-20 -31%;
  VM L1 17.5%, -21%, -13%, Sharpe 1.13; brake L1 19.4%, -23%, -13%. Part C (rule written after A/B, before it ran):
  the runner's OWN numbers off the premium ledger's 10:00 session variance (live.ibkr.sp_leg, 2022-12-21..2025-12-31,
  759 sessions; the brake fires on 3% of days, VM below full weight on 23%): HOLDOUT 2024-05..2025-12 S&P alone, VM L1
  max DD -13.4% vs -18.9%, worst-20 -8.2% vs -12.1%, Sharpe 1.22 vs 1.19, growth 15.5% vs 20.3%; joint book -13.8% /
  -8.5% vs -17.8% / -12.4%, Sharpe 1.77 vs 1.64; the brake changed nothing on the holdout (-18.9% / -12.1%). VERDICT:
  vol_managed is the runner's default S&P-leg REPORT; brake offered; 2025-04-07 replay: weight 0.44, $1M -> $437,540 =
  1 ES + 7 MES.
- 2026-09-21 live: `live/ibkr/sp_leg.py` (har_variance_forecasts: expanding-OLS HAR on the ledger's log session
  variance, one step ahead, lstsq for a flat history; vol_managed_weight; sp_leg_target incl. dollars and floored
  ES/MES), `--sp-leg vol_managed|brake|off` (default vol_managed), `--sp-leg-notional`, journal kind `sp_leg`,
  `DayRunner._sp_leg_report` after the regime read in preflight; README 1.8; 6 new tests (causal: a variance ON asof
  changes nothing for asof; the seed ledger cut through 2025-04-04 reproduces 2025-04-07's weight). The runner trades
  nothing on it.
- 2026-09-21 the 15:30 notebook (`notebooks/atm_straddle_rv_iv.ipynb`) now carries the PER-BAR forecasts. Source: the
  Hoffman2 subsection arms' `results_<bar>.csv` (pred_adj, the adjusted-scale forecast; 13 one-bar arms per spec,
  2000-session window, OOS from 2018-01-17) pulled to `results/linear_subsection/arms_hoffman2/` (108 MB, untracked)
  and stacked by `experiments/build_subsection_yhat.py` into the notebooks' table format
  (`results/spxw_pnl/yhat_sub_{ridge,lasso,enet}_all_features.parquet`, `yhat_sub_ridge_baseline.parquet`, and the
  pooled twins `yhat_pool_*`; the all_features pooled lasso waits on Hoffman2 job 14840615). Gate: the profile B equals
  the production table's to 1e-12 on every stamp. rv_raw is CARRIED FROM THE PRODUCTION TABLE: the arms' true_raw is
  true_adj^2 x B with true_adj the spec's rolling-5/95-winsorized target, so it differs from the realized variance on
  3.6% of rows; every table must score against the same unclipped rv_raw. Stamps stored at microsecond resolution
  (nanoseconds made the day frames "not identically labelled"). The notebook applies its own causal 250-session MZ map
  to each table, as to the production forecasts; nothing of the spec's look-ahead back-transform is used. Library:
  SUBSAMPLE_ORDER / SUBSAMPLE_TWIN / SUBSAMPLE_LABEL beside an UNCHANGED MODEL_ORDER (the paper's eight columns and
  their generators are untouched); the notebook tabulates MODEL_ORDER + SUBSAMPLE_ORDER and reads the twins in one
  paired table. Common frame unchanged: 866 days, block ridge sign(s) 1.338322 exactly. RESULTS (sign(s) at mid, 866
  days): per-bar ridge (all features) 1.774 [0.88, 2.63] -- the highest of the twelve and the only forecast whose
  sign(s)-minus-always-short interval excludes zero ([0.19, 2.78], HAC t 2.19); per-bar elastic net 1.577; per-bar
  lasso 1.440; per-bar ridge on HAR + calendar 1.250 (baseline OLS 0.967). Flat on event days: per-bar ridge 2.000.
  Per-bar minus its pooled twin, same spec and window: ridge (all features) +0.87 Sharpe, mean +0.061 premium/day, HAC
  t 1.98, percentile interval [0.02, 1.74] (knife-edge, excludes zero); elastic net +0.69 [-0.18, 1.49]; HAR + calendar
  -0.15 (no exogenous columns: nothing for the per-bar fit to reweight). Against the baseline's sign(s) every forecast's
  interval still includes zero (per-bar ridge +0.81 [-0.10, 1.65]). Provenance slide cut to five lines; the eight new
  provenance rows print with the rest.
- 2026-09-21 all_features lasso complete (Hoffman2 jobs 14840615 -> 14840624 -> 14847198): the tenth bucket's tables in
  `results/linear_subsection/lassofix_hoffman2/all_features/`; fair re-score over TEN buckets, 2,080 cells, none blown:
  own coefficients per bar vs pooled -2.7% (500 d; 46 better / 6 worse of 130) and -4.8% (2000 d; 76 / 2); package
  -4.7% / -7.8%. all_features itself is the weakest bucket for per-bar coefficients (2000 d: 14:30 -22%, 16:00 -3.7%,
  midday +0..+2.6%) -- the wide design already carries clock interactions the narrower buckets lack. 15:30 sign(s),
  lasso all_features, causal back-transform, 2000 d: own 16:00-bar coefficients 1.45 / 0.98 (mid / crossed) vs pooled
  1.22 / 0.74. The pooled lasso twin is now in the 15:30 notebook (`pool_lasso`; SUBSAMPLE_TWIN complete): per-bar
  lasso minus pooled lasso, same spec, +0.24 Sharpe, HAC t 0.60, [-0.60, 1.04] -- includes zero. Notebook re-executed,
  same 866 days, block ridge 1.338322 unchanged.
- 69 (2026-09-21) the month-end override's entry clock: buy at 14:00 instead of 15:30? (`69_month_end_override_clock.py`,
  holdclose/proposals/69; gates: study 54's month-end row +2.931 on 70, study 65's 9.4863%). DISCLOSED: 14:00 was
  picked from a seven-clock scan on the same 70 month-ends; the test charges the family of clocks the runner could buy
  at (13:30, 14:00, 14:30, 15:00; x4). Paired per contract, 14:00 minus 15:30: +3.30 pts, t 1.75, one-sided p 0.042,
  x4 = 0.17 -> criterion (1) FAILS; deck +1.02 / holdout +9.91 (2 passes); random-70-session placebo p 0.009 (3
  passes; on ordinary days buying at 14:00 costs -1.04 pts, t -2.7); book guard passes (14:00 override: 11.4%/yr,
  Sharpe 1.87, max DD -4.2% vs 9.5% / 1.64 / -6.3%; Sharpe difference [-0.12, +0.57]). Per DOLLAR of premium there is
  no gain (units difference -0.015): the extra points are twice the premium deployed. Anatomy: of the 14:00 long's
  +6.2 pts, +4.6 are earned after 15:30 and +1.6 in 14:00-15:30 (ordinary days -1.2 there); MSCI-review month-ends
  +11.6 vs other month-ends +3.8; realized/implied 14:00->close 1.37 on month-ends vs 1.20 otherwise. BY YEAR the
  14:00-minus-15:30 difference is +3.7, -4.0, +6.8, -2.6, -0.2, +16.9 (2020..2025): it rests on 2022 and 2025. The
  15:30 long itself is NOT a 2020-22 artefact: 2020-22 +2.56 pts (0.25 units), 2023-25 +3.32 (0.62 units); 2024 +7.3,
  2025 +0.1. The third-Friday short strengthens over time (-1.0, +0.9, +2.9, +2.0, +3.4, +3.2 pts; hit 0.75-0.83 in
  2023-25). VERDICT: NOT ADOPTED; the override stays at 15:30; 14:00 is a lead to re-test on 2026 month-ends.
- 2026-09-22 live: XSP instrument adapter (`live/ibkr/`; `--instrument xsp`, default spxw unchanged). INSTRUMENTS table
  (option symbol / trading class / index symbol / index_scale 0.1 / tick 0.01 / default hedge ladder MES->NES), a
  hedge LADDER replacing the fixed ES-bulk-MES-remainder pair (ES/MES bit-identical over 12,006 (delta, n) pairs;
  NES = CME E-nano S&P 500, $0.50/pt, listed 2026-08-24), the premium ledger kept in S&P units (XSP observations
  scaled by 1/0.1 on the way in; variances untouched), settlement = SPX close x 0.1, a SYNTHETIC XSP replay (the SPXW
  chain / 10 on the $1 grid, labelled synthetic_xsp_from_spxw) for the code path only. Stress table needs no scale
  term (the index_scale cancels). At $60k and 10%: 2 XSP straddles (0 SPXW); 2 XSP at delta 0.3 hedge as 1 MES + 2 NES
  with residual 0 (MES alone leaves 0.05). 269 tests, lint/types/parity clean. NO XSP HISTORY IN THE REPO: XSP's
  quotes are ~5-10x wider relative to premium than SPXW's against a book edge of 3-6% of premium; the numbers are
  SPXW/10 assumptions until paper-traded. Six guessed IB definitions listed in README 1.9 / 12.
- 2026-09-22 `live/ira_weight.py`: the volatility-managed S&P weight from DAILY bars (^GSPC OHLC, Parkinson range
  variance into `live.ibkr.sp_leg.har_variance_forecasts`, the same expanding HAR; weight = min(1, median earlier
  forecast vol / today's); --cap 0.6667 = a 3x fund at 2:1) for an account the runner does not see. Validated against
  the panel weight (results/.../67/vm_weight_panel.parquet, saved from study 67's HAR on the one-minute panel) on 6,065
  sessions 2000-02..2024-04: correlation 0.933 (forecast vols 0.961), mean |diff| 0.031, both weights below 1 on 30.4%
  of sessions and one-but-not-the-other on 8.5%; a 3x daily-reset fund under the daily-bar weight grows 7.9%/yr (panel
  7.8%; held 3.4%) with covid -45% (-42%; held -77%) and 2008 -80% (-79%; -96%). Quick checks the same day (not
  studies, tmp scripts): constant leverage above 2x lowers growth (1998-2024: 1x 6.5%, 2x 9.2%, 3x 7.6%, DD -98%);
  vol-target 30% cap 3x 10.3% at -70% DD; a LEAPS-like fixed-notional 2:1 rolled yearly is WORSE than daily reset (2008
  -93%, wiped by a 50% fall), rolled monthly ~2 pts/yr better (10.9% vs 8.8%) at the same drawdowns, and cannot be
  vol-managed cheaply; the month-end last-bar SIGNED move is -11 bp (t -4.4) 1998-2019 and +3 bp (t 0.4) 2020-24 --
  the month-end edge is variance, not direction. 3 new tests (272 total).
- 2026-09-22 15:30 notebook: (i) "How often the buy signal is right, and by how much" now shows one CONFUSION MATRIX
  per forecast (sign(s) buy/short x R>0/R<=0; count, share, mean R per cell; accuracy in the title; base rate P(R>0)
  0.38 in the suptitle) in the style of the paper's per-clock figure, replacing the three-panel bar chart
  (`buy_signal_confusion.png/.csv`; `buy_signal_hitrate.png` is left on disk because `buy_signal_standalone.tex`
  still includes it). Reading: accuracy 0.54-0.55 on every forecast against 0.62 for always-short; the short cells are
  the base rate on every forecast (mean R -0.67 / +0.87..0.97); the top-left cell (buy and right) carries mean R
  +1.27..+1.41 -- the signal picks bigger wins, not more of them. (ii) NEW section 17 "Portfolio weights: the trade
  beside the S&P" (S&P close-to-close from ^GSPC, gated equal to the deck's settlement closes on all 866 days; S&P
  Sharpe 0.53 on these days): whole-sample tangency per forecast, mid and crossed -- correlation -0.03..+0.04 on every
  forecast, so the tangency Sharpe is sqrt(SR_S&P^2 + SR_trade^2): block ridge 1.44 vs 0.53 alone, per-bar ridge
  1.87; the trade takes 77-92% of the risk at mid (46-80% crossed) at 2.2-3.7 dollars of premium per $100 of index
  (1.1-2.4 crossed); Kelly 5-10% of wealth in premium (half 2.5-5%). CAUSAL weight (expanding tangency, 252-day
  warm-up, risk-scaled, long-only; 614 scored days from 2021-08-11): combination Sharpe 1.00-1.68 against 0.40 for
  the S&P alone, risk share 63-81%, every paired interval includes zero (e.g. block ridge +1.04 [-0.46, +2.55];
  causally tuned lasso +1.28 [-0.14, +2.66]). Old section 17 (one row by hand) is now 18. Common frame 866 days,
  block ridge 1.338322 unchanged. (iii) RTH-as-one-segment: never run (only the 13 one-bar and the 4 block segments
  were; blocks as scored by the spec: blk_release -6..-8.5%, blk_close -4..-7.6%, blk_core -4.2..-5%, blk_open
  -1.6..-3.1% vs pooled). Staged `cluster/submit_subsection_rth.sh` + task files (60 arms: 10 buckets x ridge/enet
  under the wave root, lasso under the lassofix root; canary baseline ridge 500), deployed to Hoffman2, the causal
  scorer now admits the "rth" segment; the human submits.
- 2026-09-22 `live_feasible` bucket (src/data/loading.py SUBGROUPS): the 16 columns a 15:30 forecaster can rebuild
  live -- ES return moments and liquidity (sumret, sumabsret, sumret3/4, sumpret2, sumbipow, sumautocov, sumvolume,
  numobs), VIX / VVIX / VIX3M, the four FOMC calendar columns; excluded by name: the constituent cross-section
  (market_ew / market_vw incl. their turnover / spread / order-flow liquidity columns), StockTwits, Cboe vol demand,
  the options channels. Staged on Hoffman2 (`cluster/submit_subsection_live.sh`: canary lasso x 500 x bar1600 under
  the lassofix root; ridge / enet packs of the 13 bars + rth + pooled under the wave root; lasso likewise under lassofix;
  scorer `subsection_score_live.sh` runs both scorers on both roots). Purpose: step 0 of the live 15:30 forecast
  build -- price the feasibility constraint before writing live code (all_features per-bar ridge 1.77 mid vs baseline
  bucket 1.25; where does the live set land). `experiments/build_subsection_yhat.py` will build its yhat tables when
  the arms are pulled. The human submits.
- 2026-09-22 CARC (USC Discovery, Slurm) staging for the live_feasible campaign: fresh root
  /scratch1/jc_905/harxhar-subsection (src, specs, experiments, notebook lib, the seven feature-panel parquets = 67 MB;
  no chain), conda env `harxhar` (py 3.11, numpy 2.3, pandas 3.0, sklearn 1.9, numba 0.65; `hpc-pi` there lacks
  pandas), account pollok_1603, partition main. `cluster/slurm/subsection_pack.sbatch` (array twin of the SGE pack:
  TASKFILE line per task, DONE-skip, canary flag), `subsection_score_live.sbatch`, `submit_subsection_live.sh`
  (canary -> afterok fleets -> afterok scorer). The old ~/harxhar on CARC is the a5990c9 master line (hpc-agent
  campaigns) and is not touched. Scheduler fence: the human submits. WHAT THE RESEARCH SAYS about the feasible
  variables (fixed lasso, fair causal scoring, own 16:00-bar coefficients, 2000 days): the close's QLIKE by bucket --
  implied_vol 0.1128 (best), sentiment 0.1155, market_vw 0.1161, vol_demand 0.1167, moments 0.1170, all_features
  0.1173, baseline 0.1174, liquidity 0.1174, market_ew 0.1175, fomc 0.1178: only the VIX family clearly beats HAR +
  calendar at the close; the whole design is no better than the baseline there. 15:30 sign(s), crossed: implied_vol
  1.40, moments 1.25, market_vw 1.12, baseline 1.04, sentiment 1.00, all_features 0.98, vol_demand 0.95, market_ew
  0.92, fomc 0.67, liquidity 0.66 (mid: 1.87 / 1.71 / 1.59 / 1.51 / 1.47 / 1.45). Ridge and elastic net (spec-scored):
  moments 1.44 / 1.43 crossed, implied_vol 1.17 / 1.34, market_vw 1.15 / 1.23, all_features 1.08 / 1.08, baseline 0.89.
  The two buckets that matter at the close -- the VIX family and ES's own higher moments -- are both live-feasible;
  the infeasible ones (cross-section, StockTwits, vol demand) add nothing over the baseline there.
- 2026-09-23 live_feasible RESULT (CARC jobs 12271756-59; canary 35 s; 88 of 90 arms done, the two pooled 2000-day
  arms of enet / lasso still running): per-bar ridge on the 16 live-reproducible columns, 2000 days, 15:30 sign(s) on
  the 866 deck days 1.683 mid / 1.217 crossed (all_features per-bar ridge 1.774 / 1.308; baseline bucket 1.250 /
  0.775; block ridge 1.338 / 0.870; the live set's POOLED ridge 0.850 / 0.373); per-bar lasso 1.576 / 1.107, per-bar
  enet 1.699 / 1.231. The live set keeps ~90% of the per-bar gain; the infeasible feeds were worth ~0.1 Sharpe at the
  close; the coefficients per bar do the work. GO for the live 15:30 forecast build. Tables
  `results/spxw_pnl/yhat_{sub,pool}_*_live_feasible.parquet` (arms in results/linear_subsection/arms_hoffman2/
  live_feasible, pulled from CARC, untracked); notebook tags sub_live_{ridge,lasso,enet} (+ pool_live_ridge twin;
  the lasso / enet twins land with CARC's last two arms); notebook re-executed, 866 days, block ridge 1.338322
  unchanged. The causal scorer now scores the whole-session ("rth") arm bar by bar; deployed to both clusters.
- 2026-09-23 RTH-as-one-segment (Hoffman2 jobs 14875235-37, 60 arms; causal scorer patched to score the "rth" arm
  bar by bar; tables in results/linear_subsection/rth_hoffman2/): own coefficients vs the 48-bar pooled model, same
  causal calibration, 10 buckets x 13 bars, 2000 days -- whole session as ONE vector ("rth") -1.75% ridge / -1.86%
  enet / -1.85% lasso (78-87 of 140 cells better, 1-3 worse); one vector PER BAR -3.47% / -4.83% / -4.75%. At the
  15:30-16:00 bar (2000 d, mean over buckets): rth -5.5..-6.0%, one-bar -8.6..-9.5%. So about 40% of the per-bar QLIKE
  gain (about 60% at the close) is "stop fitting the overnight rows"; the rest is the bar's own coefficients. TRADING
  (15:30 sign(s), crossed, causal back-transform, 2000 d, mean over buckets): own 16:00 bar 1.00 / 0.92 / 0.85 (lasso /
  enet / ridge), pooled 0.72 / 0.68 / 0.73, rth 0.70 / 0.62 / 0.68 -- the whole-session model trades like the pooled
  one; only the bar's own coefficients trade better. Exception all_features: rth 1.18 / 1.13 / 1.35 beats its own bar
  0.98 / 0.86 / 1.20 (the widest design is data-starved on one bar). The lasso rth arm at 500 days blew in ONE bucket
  (vol_demand: 7 of its 13 bars at QLIKE up to 1.78 -- the lam2 = 0 pathology on a wider row set; excluding them the
  500-day lasso rth mean is -2.28% on 133 cells); the 2000-day lasso is clean. VERDICT: for the 15:30 trade the model is the per-bar one on a
  live-feasible or implied_vol/moments design, not a session-only refit. live_feasible twins complete (CARC 90/90;
  the CARC scorer needed the deck parquet shipped; its lf_score job failed on that and was re-run by hand).
- 2026-09-23 STUDY 70, chain features for the sign at the close (`writeup/intraday_proposals/70_chain_features_close_sign.py`,
  results/atm_straddle_intraday_holdclose/proposals/70/). Question: the forecast reaches the market through VIX / VVIX
  / VIX3M; does the 0DTE chain itself, read at 15:30, carry anything the per-bar live-feasible ridge does not? Six
  features from the chain only (x1 15:00 -> 15:30 change of the implied slice, x2 put-call skew from the two legs
  re-inverted with Black-76, x3 realized-so-far vs the slice, x4 premium richness vs the trailing median, x5 month-end,
  x6 third Friday), 1216 of 1279 sessions (803 deck days 2020-01 .. 2024-04, 413 HOLDOUT 2024-05 .. 2025-12 unseen
  by every forecast). Gates: chain-built R = deck R on 866 days (2e-7); the two legs sum to the package mid on 1279 of
  1279 sessions; the vectorised inversion equals live.ibkr.pricing on all 15348 cells; re-inverted skew vs the
  vendor's per-leg quotes correlation 0.855 (the vendor IV is bisection-censored, not a gate). A: alone, every feature
  AUC 0.48-0.53 for R > 0; the only large top-quintile mean is the month-end flag (+0.52 on, -0.06 off; holdout +0.45).
  B (pre-registered criterion 1, deck days, expanding logistic refit every 21 sessions after a 252 warm-up, 551 scored):
  R0 sign(s) 1.781 mid / 1.421 crossed; R1 logistic on s + chain 1.647 / 1.256, dSharpe crossed -0.16, interval
  [-1.96, +1.63] -> chain features ADD: FALSE. C (criterion 2, chain only, no forecast): frozen logistic fitted on the
  deck -> HOLDOUT crossed 0.844 vs always-short 0.376, interval on the difference [-0.32, +1.48], placebo p 0.051 ->
  chain-only rule SUPPORTED out of sample: FALSE; it buys 4% of holdout days and its coefficients are the month-end
  flag (0.19 standardized) and noise (|coef| <= 0.08). ADDED AFTER seeing those rows (reported, not a criterion): the
  P > 1/2 threshold buys 6-10% of days against sign(s)'s 42% because P(R > 0) is 0.38, so OLS of R on the same
  features with "buy if E[R] > 0" was run too -- worse everywhere: B 0.58 / 0.22 with chain, 0.02 / -0.34 on s alone
  (buying 21% of days); C holdout -0.17 frozen, -0.42 expanding, vs always-short 0.38. A fitted intercept moves the
  buy threshold off zero and the zero-crossing is where the trade lives: sign(s) at s = 0 (forecast vs implied) is a
  structural threshold that no fitted map on s recovers. VERDICT: nothing in the 15:30 chain adds to the per-bar
  live-feasible forecast; the calendar flags are already in the live book (month-end long, third-Friday size). The
  live build stays a forecast of rv from the 16 feasible columns + sign(s) at zero.
- 2026-09-23 STAGED (CARC, human submits `bash cluster/slurm/submit_mfiv_har.sh`): two forecast-input questions the
  user actually asked. (a) THE VIX FEATURES FROM THE CHAIN. `vix`, `vvix`, `vix3m` are Cboe prints on the 30-minute grid
  (VIX ~19 bars/day, the others RTH only) and the feed ENDS 2024-02-12 -- the deck's last 2.5 months carry the neutral
  fill. The chain we hold is 0DTE only (hours_to_expiration in [-3, 6.5]), so the 30-day / 3-month / VIX-options
  objects cannot be rebuilt from it; the VIX *calculation* at the 0DTE horizon can, and already existed as the every-bar
  strip book's C_t (experiments/spxw_mfiv_everybar.py) -- used as a trading benchmark, never as a regressor. New
  `experiments/build_spxw_mfiv_panel.py` -> `data/spxw_mfiv.parquet` (auto-merged by load_raw_data): mfiv0_annvol
  (100 sqrt(C/T), VIX units; named WITHOUT the vix stem because the 0DTE annualized vol climbs mechanically 35 -> 85
  through the day and must be diurnally divided, unlike the clock-free 30-day VIX), mfiv0_perbar_rv (C / remaining
  bars, target units, sqrt + diurnal), mfiv0_vvix (trailing-12-stamp std of d log annvol; flat across the clock). Gate:
  C = the strip book's C_t on all 10,455 stamps (|dC| 0.0, n_otm exact). Remaining time from the chain's own
  hours_to_expiration (half-days settle at 13:00); 72 frozen stamps dropped; 15,936 stamps on 1,291 sessions
  2020-01..2025-12 in results/spxw_pnl/mfiv_panel_diag.parquet, 10,891 rows written to the panel (the feature panel
  ends 2024-04-30, so the grid does not grow and the holdout is out of this pipeline's reach). Buckets (outside
  ALL_FEATURES like OPTIONS_FEATURES): mfiv_only (3), live_feasible_mfiv (16: the Cboe prints swapped for the chain's
  numbers), live_feasible_plus_mfiv (19). Arms: 3 buckets x ridge / enet / lasso x 13 bars + rth + pooled, tw 500 ONLY
  (the chain starts 2020; there is no 2000-day window), scored against the incumbent live_feasible tw500 arms on the
  SAME rows 2022-01 .. 2024-04. Pre-registered caveat: the regressor is nearly the quantity the trade is measured
  against; report pct_buy beside the Sharpe -- the trade lives only if the fit learns rv = beta * MFIV with beta != 1.
  (b) HAR GRANULARITY OF THE PER-BAR REGRESSION: never measured in the production pipeline (every per-bar arm used the
  powers-of-5 ladder in 48-bar units on the full series; the spec had no lag axis). Spec now takes HPC_KW_HAR_BASE /
  HPC_KW_HAR_LAGS -> run_executor(har_lags=...) (empty = production, bit for bit); the pack sbatch passes HAR_BASE and
  LAG_SCOPE through. Arms: live_feasible at bar1430 + bar1600, three estimators, tw 500 and 2000, three variants --
  base-2 (12 rungs to 2048), base-3 (8 rungs to 2187), and lag_scope=intra (the same-clock daily ladder 1/5/25/...
  sessions of the bar itself). Each variant root borrows the production pooled arms (base-5, global) as the causal
  scorer's reference, so every ladder is measured against the pooled model the incumbent per-bar arms were. Study 53's
  session-anchored ladder (RTH rungs + the overnight bar as its own regressor) is NOT among them: it needs an
  overnight-RV column and a leakage-safe executor change; deferred, stated. Evidence in hand: floor-era pooled sweep
  base-2 < base-5 monotone (DM -10.6); study 53's session-anchored proxy encompasses the ridge at every clock; the RTH
  campaign's 40% of the per-bar gain from dropping overnight ROWS while the ladder still averages overnight BARS.
  Canary mfiv_only x lasso x 500 x bar1600 -> afterok fleets (12 + 6 + 3 x 6 arms) -> scorer
  (cluster/slurm/subsection_score_mfiv.sbatch). Shipped by cluster/slurm/ship_mfiv_carc.sh; remote md5s match, the
  panel loads on CARC with the three columns. SUBMITTED by the user 2026-09-23: CARC jobs 12278584 (canary), 12278585
  (mf_re, 12), 12278586 (mf_lasso, 6), 12278587-89 (har_b2 / har_b3 / har_intra, 6 each), 12278590 (mf_score).
- 2026-09-23 RESULT (all 126 arms DONE in 35 min, canary 30 s; arms pulled to results/linear_subsection/mfiv_carc,
  untracked; fair comparison `experiments/compare_mfiv_harlag.py` -> compare_mfiv.csv / compare_harlag.csv there; same
  causal back-transform and 15:30 trade as the campaign scorer, QLIKE paired on identical rows, block-bootstrap
  interval on the daily difference).
  (a) THE CHAIN'S OWN IMPLIED VARIANCE DOES NOT IMPROVE ON THE CBOE PRINTS. tw 500, rows 2022-01-03 .. 2024-04-30, 550
  deck days; incumbent live_feasible per-bar arms there: ridge 1.92 / 1.56, enet 2.02 / 1.65, lasso 2.02 / 1.65 (mid /
  crossed; block ridge 1.48 same days). mfiv_only (HAR + calendar + the three chain columns, no Cboe prints): QLIKE at
  the close +13.0% ridge / +11.1% enet / +9.0% lasso, every interval excludes zero, worse at 12 of 13 bars; trade
  0.40 / 0.04, 1.16 / 0.79, 0.92 / 0.56 -- the horizon-matched strip is a far worse regressor than the 30-day index.
  live_feasible_mfiv (prints swapped for the chain's numbers): close QLIKE +4.5% / +1.8% / +0.5% (intervals include
  zero), 13-bar mean +0.3 / +0.7 / -1.0; trade 1.98 / 1.62, 2.02 / 1.65, 2.01 / 1.64 -- unchanged. live_feasible_plus_mfiv
  (both): close QLIKE +0.06 / +0.06 / -0.005% (the lasso interval is [-0.0000, +0.0000]: it zeroes the chain columns
  out), trade ridge 1.62 / 1.26 (worse), enet 2.16 / 1.79 (better), lasso identical -- noise both ways. The rth arms
  trail their own bars at the close as before. VERDICT: keep vix / vvix / vix3m; the live 15:30 build reads the Cboe
  prints (and needs a VIX feed that does not stop at 2024-02-12 -- the deck's last 2.5 months ran on the fill).
  Candidate reasons, untested: at 15:30 the to-close strip is one bar of variance read off wide-spread OTM quotes;
  the 30-day index is a smooth level whose HAR ladder carries the regime, which is what a next-bar regression wants.
  (b) THE HAR LADDER IS NOT A LEVER FOR THE PER-BAR REGRESSION; THE SAME-CLOCK LADDER IS RUINOUS. live_feasible,
  bar1600, vs production base-5 on common rows (2906 at tw 500 from 2012-10, 1406 at tw 2000 from 2018-09): base-2
  QLIKE -2.1 / -1.2 / -0.1% (ridge / enet / lasso) at tw 500 and +3.8 / +0.7 / -0.0% at tw 2000; base-3 -3.8 / -2.5 /
  -1.2% at tw 500 (the ridge interval [-0.0097, -0.0001] is the only one excluding zero) and +3.3 / +0.2 / +0.2% at
  tw 2000; bar1430 within +-1% at tw 500, base-3 +5..7% worse at tw 2000. Trade at the 16:00 bar (866 deck days):
  base-2 tw 500 1.68 / 1.21 vs base-5 1.27 / 0.81 (ridge), 1.41 / 0.94 vs 1.25 / 0.78 (enet), 1.34 / 0.87 vs 1.38 /
  0.91 (lasso); tw 2000 ridge 1.34 / 0.87 vs 1.90 / 1.44 (worse), enet 1.75 / 1.29 vs 1.41 / 0.94, lasso 1.79 / 1.33
  vs 1.83 / 1.36; base-3 worse than base-5 at 5 of 6 trade cells. No consistent sign across estimator x window; the
  floor-era pooled win of the denser ladder (-0.6%, DM -10.6 on 219k bars) does not show on one bar a day. The
  same-clock ladder (lag_scope=intra: lag 1 = yesterday's 16:00 bar, lag 5 = five sessions) doubles QLIKE (+86..+129%
  at both bars, both windows, intervals [+0.12, +0.24]) and kills the trade (pct_buy 53-56%, Sharpe -0.8..+0.4): the
  information is in the last bars of the same session (rungs 1 and 5 = 30 min and 2.5 h), which base-5 already has.
  VERDICT: keep the production ladder. Study 53's session ladder with the overnight as its own regressor stays
  untested but must keep the short rungs. data/spxw_mfiv.parquet stays in data/ on both machines (its columns are
  reached only through the mfiv buckets); the HAR axis stays in the spec (empty = production, bit for bit).
- 2026-09-23 DEFECT FOUND while asking why (a) made no sense: THE STRIP IS BROKEN. At 15:30 on the 866 deck days the
  model-free strip C of experiments/spxw_mfiv_toclose.py::_mfiv_one is 9.3x the deck's ATM implied slice iv_var (IQR
  5.4-16.9x) and its log-correlation with the 15:30-16:00 realized variance is 0.43 against the slice's 0.92 (VIX 0.81,
  the previous bar's RV 0.88). Decomposition on 22 sampled days: 85% of the strip integral comes from ZERO-BID strikes
  (bid 0 / ask 0.05 -> mid 0.025) beyond +-2% moneyness -- 90%+ of the strikes at 15:30 have no bid; without them the
  strip is about 1.3x the slice, as a smile-integrating strip should be. Cboe's VIX rule stops the strip after two
  consecutive zero bids; _mfiv_one takes every positive mid. Consequences: the mfiv_only failure above is the
  regressor's construction, not the chain's information; and the every-bar strip book (everybar_mtm_trades.parquet)
  and the MFIV-to-close book (mfiv_toclose.csv) priced "the listed market's implied variance" with this inflated C --
  their paper-vs-strip conclusions need re-checking with the zero-bid rule before they are quoted again. PARKED, not
  fixed here.
- 2026-09-23 CHAIN UPDATE: the user downloaded a new vendor file (Downloads/SPXW (1).zip -> SPXW.csv 2.4 GB, vendor date
  2026-09-18). Old chain + spot archived to data/archive/spxw_{chain,spot}_2026-08-13.parquet; rebuilt with
  experiments/prep_spxw.py (pyarrow parallel): 16.4M rows (was 8.2M -- about twice the strikes per stamp), 18,074
  stamps, 1291 sessions 2020-01-03 .. 2025-12-31, clocks 09:35 .. 16:00 (the old file's 09:30 clock is now 09:35).
  GATE (below): the 15:30 nearest-OTM package midpoint on the new chain equals the deck's `entry` on all 866 deck days
  (max |d| 9.5e-7) -- the deck and every 15:30 table stand. The intraday notebook (notebooks/atm_straddle_intraday)
  was regenerated by its writer but its execution was killed by the machine's memory guard (three chain-sized pandas
  jobs at once, mine); its cache key carries the chain's size + mtime so nothing stale can serve; still to run, ONE job
  at a time. Open item for that notebook: its §2b says the first usable bar is 10:00 because 9:30 had no vendor spot
  and live mids on ~40% of days; the file now carries 09:35 instead -- the usability check and the `_dead_rth` 9:30
  exemption (line ~356 of the writer) must be redone before the notebook is professor-facing.
- 2026-09-23 STAGED (CARC, human submits `bash cluster/slurm/submit_ivslice.sh`): THE ATM SLICE AS THE REGRESSOR --
  the right test of the user's question. `experiments/build_spxw_ivslice_panel.py` -> `data/spxw_ivslice.parquet`:
  proposal 43's build_chain at every clock 10:00..15:30 (guarded nearest-OTM pick, package midpoint bisected for the
  Black-76 total volatility = live.ibkr.pricing.invert_total_vol, GATE V exact on every cell), read in yearly slices in
  one process. Columns ivslice_perbar_rv (tot^2 / remaining bars, target units), ivslice_annvol (VIX units, no vix
  stem: it climbs 23.7 -> 28.1 through the day and takes the diurnal division), ivslice_vvix. 15,348 stamps on 1,279
  sessions (half sessions excluded whole), 5 no-quote cells; 10,392 rows written to the panel (to 2024-04-30). Gates
  on the 866 deck days at 15:30: package midpoint = deck entry (max |d| 9.5e-7, 0 differing); tot^2 vs the deck's
  iv_var median ratio 1.000 (IQR 0.999-1.001), log-corr 1.000 -- the deck's iv_var IS this re-inversion (the
  attach_iv_hourly_as_30min docstring's vendor-IV account is stale); log-corr with the next bar's RV 0.921 for both.
  Buckets ivslice_only (3), live_feasible_ivslice (16), live_feasible_plus_ivslice (19), outside ALL_FEATURES. Same
  arms as the MFIV campaign (3 buckets x 3 estimators x 13 bars + rth + pooled, tw 500), canary -> afterok fleets ->
  scorer (subsection_score_ivslice.sbatch); shipped by ship_ivslice_carc.sh, md5s match, the panel loads on CARC with
  10,387 slice rows. Comparison: `compare_mfiv_harlag.py` on the same 2022-01..2024-04 rows once the arms are back.
  Pre-registered caveat as before: the regressor is the quantity the trade is measured against; report pct_buy; the
  trade lives only if the fit learns rv = beta * slice with beta != 1. SUBMITTED by the user 2026-09-23: CARC jobs
  12288155 (canary), 12288156 (iv_re, 12), 12288157 (iv_lasso, 6), 12288158 (iv_score).
- 2026-09-23 NEW VENDOR FILE CARRIES EVERY ROW TWICE. The rebuilt chain's 16,397,556 rows are 8,198,778 distinct
  (timestamp, expiration, strike, cp) keys -- the old file's exact row count -- each present exactly twice with every
  column identical (checked on 15:30, 09:35 and 10:00 stamps: 960 rows = 960 exact duplicates each). The "2x strikes
  per stamp" of the first rebuild was this. It surfaced as the intraday notebook's exit cell failing (`Length of values
  (30680) does not match length of index (15340)`: the next-bar quote merge doubled). The pick-based objects were
  immune (groupby.first; the 15:30 package-mid gate passed), but n_live guards counted double and every merge on the
  key would have doubled. Fix at ingest: experiments/prep_spxw.py::_dedupe_exact keeps the first row per key after
  the combine (8,198,778 rows dropped, chain 81 MB, spot 18,074 stamps unchanged). The ivslice / mfiv panels built
  from the duplicated file are value-identical (first() picks; strip pivots with aggfunc last / drop_duplicates) and
  were not rebuilt. Intraday notebook re-executed against the deduplicated chain (alone -- one chain-sized job at a
  time on this machine); its first run on the new file also showed 09:35 now carries a vendor spot and live quotes on
  every day (17,898 straddles on 1,279 days x 14 clocks; 3 stamps with no live quote, none at the open), so §2b's
  "first usable bar is 10:00" and the `_dead_rth` 9:30 exemption are to be revisited.
- 2026-09-23 STUDY 71, conviction sizing of the sign(s) long leg (`writeup/intraday_proposals/71_conviction_sizing.py`,
  results proposals/71/; run by a subagent, reviewed). Question: the long leg is right 41% of the time; can size follow
  the chance of being right? Re-asks the 2026-09-01 "sign split is the edge, no slope" on the per-bar live-feasible
  ridge (blk2 beside it), 866 deck days, crossed. A: by quintile of s the long leg is an INVERTED U -- hit 0.24 / 0.52 /
  0.46 / 0.52 / 0.31, mean crossed -0.25 / +0.35 / +0.18 / +0.26 / -0.07, Q1 and Q5 together -71% of long P&L; s/iv_var
  the same shape; Spearman(quintile, hit) +0.21, no half of the sample grades. The only monotone grading is the
  forecast's trailing 63-session QLIKE: the quintile where the model has recently been WORST has hit 0.49, mean crossed
  +0.36 and 71% of long P&L (Spearman +0.87, both halves, both decks) -- vol-surprise clustering, the opposite of
  conviction; not pre-registered, not traded here. Flags: month-end buys +0.64 mean on 10 days (20% of long P&L),
  third-Friday buys -0.26 on 11 days, FOMC-day buys hit 0.80 on 10 days (3% of P&L). B (pre-registered, causal, 614 days
  after a 252-session warm-up, unit mean long exposure, short leg -1; supported = crossed Sharpe beats sign(s) with the
  block-bootstrap interval on the daily difference excluding zero AND placebo p < 0.05): rank of s +0.26 (1.41 vs 1.15)
  but interval [-0.018, +0.053], placebo p 0.060, keeps 11 of the 20 days that carry the P&L, drawdown 15.6 -> 18.1;
  top-quintile x2 -0.05; Kelly by quintile -0.07 (buys 250 -> 163); month-end x2 +0.07 [-0.004, +0.020], p 0.075. blk2
  the same picture (rank +0.14, Kelly -0.48). NONE SUPPORTED. C: a rank rule needs a monotone hit spread Q5 - Q1 of
  0.15-0.20 to add +0.2 Sharpe; observed 0.07 and non-monotone. VERDICT: sign(s) at size 1 stands; the one lead (size by
  the forecast's trailing surprise, not by s) needs its own causal, pre-registered study before anyone acts on it.
- 2026-09-23 LITERATURE (subagent, 12 searches; writeup/lit_vix_intraday_rv_2026-09-23.md): implied vol as an
  exogenous regressor beside RV lags is established at the DAILY horizon for S&P indices -- Busch, Christensen &
  Nielsen 2011 (HAR-RV-IV), Kambouroudis, McMillan & Tsakou 2021 (HAR + local IV index beats richer models), Clements,
  Liao & Tang 2022 (IV term-structure level/slope/curvature as HAR exogenous on SPY: the precedent for a VIX-family
  bucket), Blair, Poon & Taylor 2001 (VIX subsumes 5-min returns in ARCH); dissent Becker, Clements & White 2007 (VIX
  adds nothing beyond a rich model set at 22 days). At INTRADAY horizons the refereed models (Andersen & Bollerslev
  1997, Engle & Sokalska 2012, Stroud & Johannes 2014) carry no option-implied regressor at all; no refereed paper puts
  VIX/VVIX/VIX3M at a 30-minute stamp into an intraday HAR or reports the 15:30-16:00 bar. The only located intraday
  HAR + 0DTE-IV precedent is Ougou (SSRN 2026, unrefereed, R^2 0.815 vs 0.792 at 30 min). Vilkov 2026 and Almeida,
  Freire & Hizmeri 2025 measure the 0DTE variance risk premium from 30-min bars to the close (small unconditional
  carry, tail-dominated) -- consistent with "sign(s) is a tail trade", not a forecast-input result. Cite first: Busch
  et al. 2011, Clements et al. 2022, Engle & Sokalska 2012 (the gap).
- 2026-09-23 ATM SLICE AS REGRESSOR -- RESULT (88 of 90 CARC arms; the two pooled arms were still running and are
  not needed: the comparison is against the incumbent per-bar arms; `compare_mfiv_harlag.py --family ivslice`,
  results/linear_subsection/ivslice_carc/compare_ivslice.csv; tw 500, identical rows 2022-01-03 .. 2024-04-30, 550
  deck days; incumbent rows reproduce compare_mfiv.csv exactly). ivslice_only (slice + HAR + calendar, no Cboe prints):
  QLIKE at the close +6.0 / +6.9 / +5.9% (ridge / enet / lasso; the enet interval excludes zero), worse at 11 of 13
  bars, trade 0.88 / 1.06 / 1.12 mid -- the clean slice is a far better object than the strip (log-corr 0.92 vs 0.43
  with the next bar) and STILL a worse regressor alone than the 30-day prints: what the regression wants is the smooth
  regime level, which the slice does not carry. live_feasible_ivslice (prints swapped for the slice): close QLIKE
  +1.4 / -0.8 / -0.7% (intervals through zero), 13-bar mean -2.6 / -1.1 / -2.8%, trade 1.63 / 1.26, 2.12 / 1.75, 1.93 /
  1.56 vs 1.92 / 1.56, 2.02 / 1.65, 2.02 / 1.65 -- neutral. live_feasible_plus_ivslice (both): close QLIKE -0.8 / -2.2 /
  -1.1%, the enet interval [-0.0032, -0.0005] and the lasso interval [-0.0024, -0.0000] exclude zero, 9-10 of 13 bars
  better, 13-bar mean -2.6 / -2.9 / -2.9%; trade 1.81 / 1.44, 1.83 / 1.47, 2.02 / 1.65 -- not better. VERDICT: the
  horizon-matched slice ADDS a small, real QLIKE improvement (2-3% across the session, 1-2% at the close) beside the
  Cboe prints and nothing to the trade; the VIX family stays the IV input of record, the slice is an optional
  QLIKE add for the live build (it is free there: the 15:30 package mid is on the screen).
- 2026-09-23 STAGED (CARC, human submits `bash cluster/slurm/submit_ivrep.sh`): IV REPRESENTATION CAMPAIGN (built by a
  subagent, reviewed). `experiments/build_vix_representations.py` -> `data/vix_representations.parquet` (329,208 grid
  rows x 10 columns): impl30_perbar_rv = (VIX/100)^2 / (252 x 48) (the VIX on the target's sqrt-ratio scale: sqrt +
  diurnal division, no vix stem), vix_slope_3m = log(vix3m/vix), vvix_over_vix, vix_chg_1bar / _1d / _5d (log changes;
  the day changes are shifts WITHIN the clock, groupby(hhmm).shift -- the grid drops the closed hours around weekends,
  so 48 rows is not one day on Mondays), vix_vrp_1d / 5d / 22d = log(implied per-bar variance / trailing realized mean
  over 1 / 5 / 22 sessions), ivslice_over_vix (the 0-day / 30-day slope). Stem rules resolved through the library:
  signed columns -> identity with no diurnal division, ratios -> log, the _rv column -> sqrt + diurnal. Coverage: VIX
  columns 2006-05-22 .. 2024-02-12 (85,625 rows), slope 2009-08 .., vvix ratio 2012-03 .., slice ratio 2020-01 ..
  2024-02 (9,739). Log-corr at 15:30 with the 15:30-16:00 realized variance (1029 days 2020-24): vix +0.81 =
  impl30_perbar_rv (monotone transform), vix_slope_3m -0.73, vvix_over_vix -0.70, vix_vrp_1d -0.64 / 5d -0.50 / 22d
  -0.23, vix_chg_1bar +0.05 / 1d +0.21 / 5d +0.32, ivslice_over_vix +0.80. Buckets (R0 = live_feasible):
  ivrep_target_scale (16), ivrep_term (16), ivrep_innovations (19), ivrep_vrp (16), ivrep_slice_slope (17), ivrep_all
  (25). Arms: per-bar only (13 bars, three estimators; tw 2000 for the VIX-based buckets, tw 500 for the slice slope);
  NO fresh pooled arms -- each bucket's `none/` is a symlink to R0's pooled arms, so the CARC scorer measures every
  representation against the same pooled model (hours -> ~35 min). Canary ivrep_target_scale x lasso x 500 x bar1600
  -> afterok fleets (12 + 6 packs) -> rep_score. `compare_mfiv_harlag.py --family {mfiv,ivslice,ivrep}` (default
  reproduces today's mfiv tables byte for byte). Shipped; md5s match; the panel loads on CARC with the ten columns.
  SUBMITTED by the user 2026-09-23: CARC jobs 12289445 (canary), 12289446 (rep_re, 12), 12289447 (rep_lasso, 6),
  12289448 (rep_score).
- 2026-09-23 VILKOV (SSRN 4641356; read from the public package by a subagent; writeup/lit_vilkov_0dte_2026-09-23.md).
  The package supersedes the paper: KNOWN-ISSUES (Aug 2026) records a 100x cost bug (half-spread charged at 1/100) --
  after the fix "no strategy or basket retains a positive net Sharpe"; his conditional 1.18 / 0.93 and 1.12 / 0.82
  are retracted, the corrected best is about 0.07 net. Construction: Cboe 30-min bars 09/2016-01/2026, VIX formula on
  Akima-interpolated mids within +-2% moneyness (NO zero-bid rule -- the window, not zero-bid discipline, keeps his IV
  near the ATM slice and clear of our 9x strip problem), RV from 1-min returns to 16:00, unannualised, synthetic
  exactly-ATM straddle, unhedged, entry-only costs (net = sign x gross - c). Per-bar VRP: the text carries no per-bar
  numbers and the shipped figure is blank; the paper does not claim the close is richest. What it does carry for us:
  the 15:00 -> close regression of straddle PnL on IV (t -2.7 / -3.8) and RV (t +2.5 / +5.2), R^2 0.20-0.31 vs 0.06 at
  10:00 -- PnL = beta (RV - IV), the sign(s) construct ex post; and the tail message (ES1% 1.61% of spot vs |mean|
  0.02%/day, worst day -2.67%, worst 5 days -7.77%; long-straddle loss probability 69.9%). Not in the paper: 15:30
  entry as a trade, any realized-vs-implied sign rule, month-end / third Friday / FOMC / day-of-week conditioning,
  delta hedging, capacity, margin. His unconditional exactly-ATM 10:00 straddle has SR -0.03 mid and 2020-21 long SR
  -0.78; his classifier is 92% short the straddle -- the opposite default to our long-biased tail trade, at a clock
  we do not trade. To check in our data (light): our 15:30 implied variance on his +-2% window vs the slice and the
  strip; his Panel-C regression at 15:30 on the deck for a comparable R^2; ES1% / worst day / worst 5 days / max DD of
  sign(s) in % of spot. Cite for the definition stack and the small-VRP / tail-dominated message; never his retracted
  Sharpes.
- 2026-09-23 IV REPRESENTATION -- RESULT (CARC 12289445-48, 235 arms in 38 min, no fresh pooled arms;
  `compare_mfiv_harlag.py --family ivrep`, results/linear_subsection/ivrep_carc/compare_ivrep.csv). Reference R0 =
  live_feasible per-bar arms at the same estimator and window; VIX-based buckets at tw 2000 on the 866 deck days (R0
  trades ridge 1.90 / 1.44, enet 1.41 / 0.94, lasso 1.83 / 1.36 mid / crossed), the slice-slope bucket at tw 500 on
  the 550 days from 2022-01 (R0 1.92 / 1.56, 2.02 / 1.65, 2.02 / 1.65). QLIKE at the close, ridge / enet / lasso:
  target-scale -1.8 / -0.5 / -0.6% (ridge interval [-0.0061, +0.0006]); term structure -0.8 / -0.7 / -1.3% (enet
  [-0.0021, +0.0006], lasso [-0.0042, +0.0008]); innovations -1.6 / -1.4 / +0.7% (enet better at 13 of 13 bars);
  VRP -0.9 / -0.0 / -0.4%; all -1.1 / -1.2 / +1.1%; slice slope +1.3 / -0.8 / -0.7%. EVERY interval includes zero.
  Trade at the 16:00 bar: no representation beats R0's best cell (ridge 1.90 / 1.44 at tw 2000; enet / lasso 2.02 /
  1.65 at tw 500) -- ridge falls in every bucket (target-scale 1.64 / 1.17, term 1.75 / 1.28, innovations 0.97 / 0.50,
  VRP 1.65 / 1.19, all 1.15 / 0.69); enet, R0's weakest, rises in most (target-scale 1.55 / 1.08, term 1.75 / 1.28,
  innovations 1.61 / 1.14, all 1.69 / 1.22, slice slope 2.12 / 1.75); lasso stays within +-0.25. The representations
  reshuffle which estimator trades best without lifting the ceiling; on 866 days a +-0.3 Sharpe move is inside the
  day-block interval. Innovations (log changes) are the informative case: they buy QLIKE at every bar for the elastic
  net and destroy the ridge trade (12 rungs x 3 change columns the ridge cannot shrink). VERDICT: the log-level VIX
  family with the production ladder (R0) is not beaten by any representation -- the VIX on the target's sqrt scale,
  the term slope, the changes, the VRP and the 0d/30d slope are each worth at most 1-2% QLIKE at the close, never
  significant, and nothing at the trade. Representation of IV is a non-lever at this sample size; keep R0.
- 2026-09-23 STUDY 72, vol-surprise clustering and the sign(s) long leg (`writeup/intraday_proposals/72_vol_surprise_clustering.py`,
  results proposals/72/; subagent, reviewed). Follows study 71's lead: the long leg's only monotone grader was the
  forecast's trailing 63-session QLIKE. A (what it measures; 866 deck days, buy days, sub_live_ridge / blk2): unsigned
  QLIKE_63 Spearman(hit) +0.87 / +0.80 and 71% of long P&L in its top quintile, holding ex 2020-03..06 and WITHIN every
  VIX-level tercile (mid-VIX hit 0.20 / 0.53 / 0.50 by surprise tercile); Spearman with the VIX level on buy days +0.09
  -- not a regime proxy. The SIGNED forecast surprise log(rv / rv_hat) grades the wrong way (-0.30) and is positive on
  1.5% of scored days (exp of its 63-day mean: median 0.88, IQR 0.85-0.93): the recalibrated forecast runs above
  realized, never below -- the "forecast lags upward, sign(s) under-buys" mechanism is REFUTED; B's causal
  multiplicative bias correction cuts buys 41% -> 24% and moves crossed Sharpe 1.15 -> 1.22 (63) / 0.46 (21), blk2
  worse. What grades is the recent DISPERSION of forecast errors (vol-of-vol clustering, which a long straddle is
  convex in), riding with realized-has-beaten-implied (isur_63 +0.67, rho(VIX) -0.71). C (pre-registered, causal, 614
  days from 2021-01, exposure-matched, short -1; supported = block-bootstrap interval on the daily difference excludes
  0 AND placebo p < 0.05 AND both halves positive): NONE. Best rank of QLIKE_63 +0.23 crossed (1.38 vs 1.15), interval
  [-0.015, +0.059], placebo p 0.27, halves -0.26 / +0.15, keeps 15 of the 20 days that carry the P&L; tercile 2/1/0.5
  +0.15; gates +0.14 / -0.71 (the 2021 half of every gate is always-short: the trailing-252 tercile edges are 2020's
  COVID QLIKEs); all 21-session variants lose (-0.08..-0.69); blk2 rank +0.04. VERDICT: sign(s) at size 1 stands; the
  surprise is a real descriptive regularity and not a tradable rule. NOTED, NOT PURSUED (the 2026-09-01 rule: don't
  fish book Sharpe through the smear): on sub_live_ridge the raw sign rule sign(yhat^2 x baseline - iv_var) scores
  2.03 / 1.67 vs the recalibrated deck's 1.51 / 1.15 over the same 614 days -- the raw sqrt-ratio forecast squared is
  Jensen-low, so it drops the marginal (Q1) buys that study 71 showed lose; a recalibration question, to be tested as
  a pre-registered rule with its own placebo before anything is read into it (study 74, part D).
- 2026-09-23 STUDY 73, Vilkov's constructions on our deck (`writeup/intraday_proposals/73_vilkov_checks.py`, results
  proposals/73/; subagent, reviewed). Gates: chain 15:30 legs and spot = deck on 866/866 (0.0), strip C = MFIV panel
  diagnostics on every stamp (0.0), R and signal reproduce on both decks. (i) His IV = VIX formula on LISTED strikes
  with K/S in [0.98, 1.02] (no interpolation, r = 0, F from min|C - P|, one-sided dK at the edges): median 1.23x the
  ATM slice (IQR 1.13-1.38; 1.17x with zero-bid strikes dropped), 0.13x the full strip; zero-bid strikes carry 4% of
  the window vs 85% of the strip -- the moneyness window is what keeps his IV sane; log-corr with the 15:30-16:00
  realized variance 0.921 = the slice's 0.921 (strip 0.427). (ii) His Panel C at 15:30 on our deck, NW(3): PnL in % of
  spot on IV and realized RV: IV t -6.49, RV t +9.07, adj R^2 0.363 (his 15:00 -> close 0.20-0.31, t -2.7/-3.8 and
  +2.5/+5.2) -- same structure, stronger; ex ante with rv_hat the signs flip (IV +5.5, rv_hat -4.4; collinearity), the
  linear gap s has no slope (t -0.8, R^2 0.00), the traded dummy 1{s > 0} carries +0.24 of R (t +2.9) -- study 71's
  "sign split, no slope" again. (iii) In his units (one straddle per unit of spot, PnL in % of spot, sqrt(252)):
  sign(s) mid +0.010%/day Sharpe 0.66, ES1% 1.23, worst day -3.03, max DD 6.3, skew -3.0; crossed +0.003%/day Sharpe
  0.20, ES1% 1.29, worst 5d -3.50, max DD 7.9, loss prob long 60% / short 37%; always-long loss prob 61.8% (his 69.9%);
  always-short crossed -0.56; his basket -0.0215 / -0.97 / 1.61 / -2.67 / -7.77 / 25.9. FLAG: the headline 1.68 / 1.22
  is a PREMIUM-UNIT book (one premium dollar a day, implicitly vol-sized: fewer contracts when premium is high); at
  constant notional the same positions are 0.66 / 0.20 with skew -3 to -4 because the short leg's tail bites on the
  highest-premium days -- the writeup and the live sizing must state which convention is quoted, and the live book
  should size by premium, not by contracts, if it wants the research numbers. Caveats: his 10:00 vs our 15:30,
  synthetic exactly-ATM vs listed nearest-OTM, 953 days 2016-24 vs 866 days 2020-24.
- 2026-09-23 STUDY 74, tail-day capture (`writeup/intraday_proposals/74_tail_day_capture.py`, results proposals/74/;
  subagent, reviewed). Question: sign(s)'s P&L is ~20 of 866 days -- can the tail be captured better than by the mean
  sign rule? A: sign(s) bought 12 of the 20 largest R days (26% of its crossed P&L; blk2 11 / 20%), 73 of the 137
  R > 1 days (34%); the 8 missed had s < 0. Ex ante, NO 15:30-observable feature separates R > 1 days (best s, AUC
  0.58 [0.53, 0.64]; last-bar variance, day range, VIX, VVIX, term slope, every calendar flag at 0.5). The extreme
  tail (top 2.5% of |R|) is separated by isur_63 / s / s-over-iv (0.64-0.65) and INVERSELY by premium-over-spot
  (0.31), VIX3M (0.34) and |day return so far| (0.33): the largest R come on quiet, cheap-premium days -- partly the
  premium denominator, partly realized-has-beaten-implied. B (pre-registered L2 logistic on a feature list fixed in
  advance; 560 causal days 2021-08 .. 2024-02; exposure-matched; short -1): every rule loses -- break-even p* -0.97,
  top quintile -0.76, sign(s) AND p-hat -0.33 [-0.107, +0.074] placebo 0.42, sign(s) OR break-even -0.87; on the
  trailing 97.5% target the one positive cell (+0.12) has its interval through zero; p-hat is s with noise and every
  departure drops 4-13 of the 20 days that carry the P&L. C: at 30 minutes the 1% and 2% OTM strangles are tick-priced
  (0.23 bp of spot) and EXPIRED WORTHLESS on every buy day of every rule; the 0.5% wing (added after) hits 2% of days,
  P&L per unit spot -0.80 bp/day vs the straddle's -0.35 -- no cheaper convexity than the straddle exists at this
  horizon. D (the raw smear-free sign rule study 72 flagged, pre-registered here): sub_live_ridge 2.03 / 1.67 vs
  1.51 / 1.15 (+0.51 crossed, both halves +0.5) but interval [-0.015, +0.090], placebo p 0.30, and blk2 -0.00
  [-0.056, +0.053]; the raw forecast is Jensen-low (median 0.94x) and drops 41 of 50 Q1 buys (mean R -0.45) and 17 of
  50 Q2 buys (+0.37). NOT SUPPORTED. VERDICT: the tail is a regime sign(s) already stands in when s > 0 (cheap
  implied, recent surprises); within it the day is not separable; sign(s) at zero with the straddle stands.
- 2026-09-23 LIVE SIZING BY PREMIUM (operator's call after study 73's units flag). live/ibkr: new
  `sizing.contracts_for_outlay(capital, fraction, premium_points, index_multiplier)` = floor(capital x fraction /
  (ask x multiplier)) -- the research book's premium units and, for a LONG straddle, the count whose worst case
  (its premium) is the loss budget; it does not apply to a short (loss not bounded by premium; the stress table
  stays). `month_end_long_size` gains ``"premium"`` and it is the DEFAULT: the month-end long is sized at 15:30 off
  the ask the order pays, `month_end_long_fraction` (None = the stress fraction, so the long's worst case equals the
  short program's budget), optional `max_long_straddles` ceiling, `--n` still overrides; the outlay <= budget by
  construction and the hard check stays; the short program's stress count at the size clock is still journaled with
  `reference_only=True`. ``match_short`` (proposal 55's measured variant) remains offered. CLI
  --month-end-long-size / --month-end-long-fraction / --max-long-straddles. README §"How it is sized" rewritten
  (premium default, the 28x figure now describes the default, liquidity untested on the tape, the resting limit at
  the ask is the liquidity control). Tests: contracts_for_outlay arithmetic and refusals; the premium default sizes
  n = floor(budget / (ask x 100)) at 15:30 with the outlay within one contract of the budget; max_long_straddles caps;
  the match_short tests run with the mode set explicitly. 276 tests pass. The 15:30 sign(s) book (live build steps
  2-7, not yet built) inherits the rule: long leg by premium, short leg by min(premium count, stress count).
- 2026-09-23 STUDY 76, Kelly of the LONG leg of sign(s) -- the Heaviside part 1{s > 0} (`writeup/intraday_proposals/
  76_kelly_long_leg.py`, results proposals/76/; subagent, reviewed). f = fraction of capital paid as premium on a buy
  day (~101 buys/yr), study 66's root of E[R / (1 + f R)] = 0; gates: R and s reproduce on both decks, the path
  ensemble's growth-optimal f 0.065 = the analytic 0.066. A: sub_live_ridge crossed (bought at the ask), 348 buys,
  hit 0.40, mean R +0.091: f* = 0.066, day-block interval [0.000, 0.153], half-Kelly 0.033; annual growth 33% at f*,
  25% at f*/2, 24% at the live 0.10, which is 1.53x f*; pessimistic f* (mean R shrunk 1 / 2 SE) 0.015 / 0.000;
  halves 0.080 / 0.055; mid 0.092 [0, 0.186]; blk2 0.044 [0, 0.131], live 2.27x f*, growth at 0.10 NEGATIVE (-6%;
  -19% in 2022-24). B: f* is the top five days -- removing them leaves 0.009 (14%), removing ten leaves 0; 21.6% of
  buys expire worthless and one more such day per fifty sends f* to 0; d f* / d(mean R) 0.82. C: the month-end long
  alone (every month-end, n 52, hit 0.54, mean R +0.43): f* = 0.443 [0.128, 0.773], pessimistic 0.221 / 0.057 -- the
  runner's 0.10 is 0.23x Kelly, inside the pessimistic band; third-Friday buys f* 0 (n 11), FOMC-day buys 0.28 with a
  useless interval (n 10). D: the short leg (sold at the bid) has min R -5.61 -> feasible f < 0.178, unbounded tail ->
  the stress table, as sized. E: 10,000 five-year paths of the long leg: at f* median 4.1x / p05 0.27x terminal
  wealth, P(DD > 50%) 0.98; at the live 0.10 median 3.0x / p05 0.05x, P(DD > 50%) 1.00, 33% of paths below start; at
  f*/4 = 0.016 median 1.9x / p05 0.94x, P(DD > 25%) 0.54, P(DD > 50%) 0.013. F: f* 0 in the bottom trailing-QLIKE
  tercile, 0.21-0.23 in the top (descriptive). VERDICT: 10% is quarter-Kelly for the month-end long it sizes today
  (keep); for the general sign(s) long leg it is 1.5-2.3x Kelly with certain 50% drawdowns -- that leg's premium
  fraction should be ~f*/4, 1.5-2% of capital per buy day, and Kelly here is an upper bound resting on five days, not
  a target. Naming: 1{s > 0} = H(s) is the Heaviside step of the gap (sign(s) = 2H(s) - 1) -- proper for the position
  indicator; the payoff is a straddle, not a step.
- 2026-09-23 STUDY 75, exploiting the vol-surprise regime correctly (`writeup/intraday_proposals/75_surprise_regime_exploitation.py`,
  results proposals/75/; subagent, reviewed). Study 72's grading re-done with CAUSAL labels (expanding quantile edges,
  >= 63 prior sessions; EWMA half-lives 21/42/63 beside the boxcar 63): the long leg's hit by boxcar-63 tercile is
  0.36 / 0.45 / 0.43, share of long P&L 0.06 / 0.50 / 0.44 -- a CALM-regime loser, not a surprised-regime winner (72's
  +0.87 used full-sample quintiles; causal labels keep about half of it; no definition grades monotonically on both
  decks). PRIMARY (pre-specified): the production MZ map (asl.second_order_mz; regime-ignored rebuild == the deck's
  rv_hat on 866/866, dev 0.0) fitted WITHIN the day's causal tercile, sign(rv_hat_reg - iv_var), 522 days from
  2021-08: median rv_hat_reg/rv_hat 0.98 / 1.01 / 0.94 by tercile; drops 20 of 207 buys, 18 of them
  bottom-s-rank-quintile, adds none; dSharpe crossed +0.18 [-0.018, +0.041], placebo p 0.56, halves +0.83 / +0.06; blk2
  +0.00 -- NOT SUPPORTED. 16 secondary regime-MZ cells: -0.17..+0.60, one passes (ewm21 x trailing-252, sub_live_ridge
  +0.60, CI [+0.015, +0.069], p 0.025) and fails on blk2 (+0.29, CI through 0) -- 1 of 16 at p 0.025. Regime-
  conditional multiplicative factor worse (-0.14 / -0.33). TWO-SIDED book: the surprise carries more for the SHORT leg
  -- (b) short only outside the top tercile lifts the short-leg Sharpe 0.93 -> 1.15 (blk2 1.15 -> 1.29), total +0.12 /
  +0.07, p 0.085 / 0.070 both decks, CIs through 0; (d) tercile sizing both legs +0.42..+0.44, matched CI [-0.005,
  +0.072], 2021 half -0.98, keeps 12 of 20 tail days; restricting longs to the top tercile keeps 6-8 of 20.
  INTERACTION: 71's inverted U in s lives in the calm tercile (hit 0.12 / 0.62 / 0.29 / 0.41 / 0.25 across s-rank
  quintiles); "skip Q1/Q5-of-s buys in the calm tercile": sub_live_ridge +0.55 (1.77 vs 1.23) [+0.011, +0.062]
  p 0.010 halves +0.99 / +0.46 keeps 19 of 20 -- PASSES; blk2 +0.02 p 0.36 -- FAILS; one deck, 45 buys, not
  pre-registered before this study: not for the book. POWER: minimum detectable dSharpe at 95% on 522 days is 0.40 for
  the primary (0.36-0.53 for the better rules, 0.8-1.5 for the gate/two-sided rules); +0.23 needs about 1,600 days.
  VERDICT: sign(s) at size 1 stands; the surprise is a real calm-regime regularity whose tradable form is subtractive
  (refuse marginal buys when the model has been accurate) and whose effect is below this sample's detection floor --
  "too small to certify", not "absent". Also seen: sign(raw - iv_var) is +0.41 on sub_live_ridge and -0.43 on blk2
  over the same 522 days (study 74 D owns that test). STATUS (operator, 2026-09-23): the regime line is CLOSED for the
  15:30 sign(s) book and stays OPEN for the INSURANCE book -- studies 72 and 75 tested the surprise on the 15:30 trade
  only; their one consistent direction was on the short leg (a surprised regime is when the short's tail bites), and
  the 13:30 delta-hedged short program has never been gated by it. Its existing brake is proposal 50's trailing
  realized-variance percentile; the trailing forecast surprise (QLIKE_63, causal expanding terciles) is a different
  gate and is to be tested on the insurance book's own ledger (hold-to-settlement, hedged every 30 min, net 0.5 bp)
  against that brake, same criteria as 75, before anything changes in live/ibkr.
- 2026-09-23 MONTH-END LONG: BOUNDED CHASE (live/ibkr; operator's call; subagent, reviewed). The research number assumes
  a full fill at the 15:30 quoted ask and the deck says paying up is cheap against missing the day (52 month-ends:
  mean R +0.43 at the ask, +0.39 at ask + 2 ticks, +0.36 at ask + 5%, break-even ~33 ticks; one tick = 1.3% of the
  median 7.7-pt straddle), so the order now steps one tick at a time through the ask (IBBroker's own stepping,
  passive_wait_s between steps, never a market order), bounded by the smallest of: `month_end_long_max_cross_ticks`
  (None = the short book's spread cap ceil(spread/tick)+1 capped at max_cross_ticks; 0 = the old rest-and-cancel),
  `month_end_long_max_chase_pct` (0.05 of the quoted ask), the loss budget (no step past n x price = budget; the
  premium size is taken off ask x (1 + pct) so the budget has room for the chase), and `month_end_long_deadline`
  (15:35; the unfilled remainder is cancelled, `Fill.deadline_hit`). Entry journals quoted_ask_1530 / avg_fill /
  chase_ticks_used / chase_cap_ticks / chase_cap_source / slippage_pts / slippage_premium_units / deadline_hit so the
  live ledger measures the chase against the +0.43 the research assumed. FakeFaults.needs_ticks drives the tests; 282
  pass. OPEN, before any live combo order: (a) on a cheap straddle the 5% ceiling allows one tick (WORST: ask 3.65,
  tick 0.10) -- by design, stated; (b) PRE-EXISTING: `round_to_tick` on a half-tick package price (3.65 + 0.10 ->
  3.7499.. -> 3.7) lets IBBroker's step ladder move half a tick off such a base -- the package tick rule ($0.10 at or
  above $3) vs two legs each on $0.05 needs a decision (price the combo on the legs' grid or round up) and a test.
- 2026-09-23 THE LIVE 15:30 BOOK BECOMES A LONG-ONLY CALENDAR SIGNAL (operator): no short side; the instruction reaches
  the operator as a Google Calendar event computed each session and executed by hand on Robinhood (XSP index options
  -- European, cash-settled to SPX/10 at the official close, 60/40; SPY was ruled out: physical settlement, Robinhood's
  forced close of ITM options before expiry, the 16:15 window, ordinary tax); free feeds; GitHub Actions; sizing by
  premium at study 76's half-Kelly (general long leg 0.033; month-end 0.10 for the first quarter, then 0.22).
  HISTORY ONCE, THEN RANK-1 DAILY: the vendor panel ends 2024-04 (Cboe 2024-02); the gap is a ONE-TIME purchase
  (writeup/history_purchase_2026-09-23.md, priced by a subagent from the vendors' pages): Databento GLBX.MDP3
  ohlcv-1m ES.FUT + ES.v.0 2024-04 -> now, covered by the $125 new-account credit (personal use, instant approval;
  ~100-200 MB); FirstRate Data "US Indices" bundle (VIX / VIX3M from 2008, VVIX from 2012, SPX; 1-min; cart price,
  low hundreds inferred; decline the $59.95/mo updates); Cboe DataShop 15-second CGIF ticks as the audit-grade
  alternative (cart price). No free source of intraday VVIX / VIX3M exists anywhere. After the fill, the free feeds
  append one session a day and RollingLeastSquares / the trailing MZ map update incrementally -- the refit rhythm of
  the research arm, no frozen coefficients. THE LATENCY WALL (Yahoo's exchange-delay page, verified): Cboe indices
  15-min delayed, CME 10-min delayed, S&P indices real-time -- so at 15:30 the free feeds do NOT carry the 15:00-15:30
  ES bar or the 15:30 Cboe prints. Three input modes carried in the build: `ibkr` (real-time through the broker code;
  the research construction; a few dollars a month of IBKR data), `free_substitute` (real-time ^GSPC 1-min for the
  last bar's moments + the ~15:15 Cboe prints; a MODEL CHANGE to be validated on the purchased history before use),
  `free_delayed` (trade at ~15:45, not the research object). The gate on a month-end long at 15:30 needs no forecast
  and is free-feed clean today.
- 2026-09-23 STUDY 77, run lengths and block bootstrap of the month-end long and the joint long-only close book
  (`writeup/intraday_proposals/77_runs_and_block_bootstrap.py`, results proposals/77/; subagent, reviewed). Replaces
  the iid resample behind the 22% month-end fraction. Gates: 71's loader on both decks; 52 month-ends = one per
  calendar month (the README's 70 run to 2025-12 on the ledger window); block-1 reproduces the quoted iid table
  (growth 0.762 LOG rate -- the "+76%/yr" quoted in chat was continuously compounded, +114% simple; 0.605 / 0.071 /
  0.166). A: losers do NOT cluster -- month-end long longest loser run 3 (iid P(>= 3) 0.96), runs test z +0.33 p 0.75;
  general long leg longest 9 (iid P 0.77), z +0.50 p 0.62; autocorrelations lag 1-3 inside block intervals, lag-1
  slightly negative (-0.10 monthly R); month-end hit by year 0.42 / 0.58 / 0.67 / 0.42 / 0.75, mean R +0.04 / +0.24 /
  +0.46 / +0.73 / +1.18. B (monthly circular blocks 3 / 6, 20,000 x 60 month-ends): at 0.22 P(DD > 50%) 0.60 -> 0.56 /
  0.38, P(DD > 75%) 0.07 -> 0.05 / 0.00, 1-yr P(loss) 0.17 -> 0.14 / 0.09, 5-yr p05 2.7x -> 3.6x / 5.9x, growth
  unchanged; a -39% peak-to-trough inside some 12-month window is ~0.90 over 5 years at 0.22 (0.07 at 0.10, 0.26-0.47
  at 0.15). C (daily block 21, joint long-only book): legs independent (monthly corr 0.18; P(general-leg month loss |
  month-end loser) 0.50 vs 0.48), month-ends ~85% of growth; (0.22, 0.033): +206%/yr simple, 5-yr p05 7.4x, P(DD >
  50%) 0.90, P(DD > 75%) 0.22, 1-yr P(loss) 0.13; the two-legged sign(s) book at 0.033 is +72% with P(DD > 50%) 0.76.
  D: joint growth-optimal (0.45, 0.066) with P(DD > 50%) 1.00; max-p05 (0.30, 0.033) with P(DD > 75%) 0.50 -- p05
  terminal is flat from f_me 0.20 to 0.30 while P(DD > 75%) climbs 0.17 -> 0.50. E: month-end mean shrunk 1 SE ->
  Kelly 0.221, at 0.22 growth +36%, p05 0.33x, P(DD > 75%) 0.35, 1-yr P(loss) 0.33; two largest month-ends removed ->
  Kelly 0.36, p05 0.86x at 0.22; at 0.15 both pessimistic cases stay out of the blow-up region (p05 0.65x / 1.1x,
  P(DD > 75%) 0.07 / 0.015) with observed-mean growth +80% simple. VERDICT: dependence does not change the 22% picture
  (it improves every tail number); estimation risk does -- 22% is half-Kelly on the observed mean and full Kelly one
  SE below it. 15% is the standing fraction that survives the estimation risk; 22% is the ceiling, earned when the
  live ledger's month-end mean holds. Operator reading (why 15 and not 10): 10 -> 15 adds +26 / +7 / +13 points of
  growth (observed / -1 SE / trimmed) with P(DD > 75%) <= 0.07 in every case; 15 -> 22 adds +35 on the observed
  mean but +3 on the pessimistic while P(DD > 75%) goes 0.07 -> 0.35 -- the knee is 15. Start at 10 for a quarter
  while the chase journal measures fills.
- 2026-09-23 LIVE 15:30 CLOSE SIGNAL, LONG-ONLY, FOR A HAND-TRADED ACCOUNT (live/close_signal; built by a subagent,
  reviewed; not yet run). The book is the Heaviside part 1{s > 0} plus the month-end long -- the short side of sign(s)
  is not traded. Each session a GitHub Actions job (crons 19:00 + 20:00 UTC, an ET-DST guard keeps the 15:00 ET one,
  sleeps to 15:30:30 inside the job) rebuilds the day's 48 panel rows from FREE Yahoo 1-minute feeds, appends them to
  a committed store, runs THE SAME spec arm the research measured (specs/causal_tune_linear.py, bar1600, ridge,
  tw 2000, the new free_feasible bucket = live_feasible minus the liquidity columns and numobs, 14 cols) on a scratch
  copy of src/ + spec + data/ extended by the store's rows, restricted to the last rows with the training halo, then
  the deck's causal MZ map (asl.load_yhat_panel_mz) -> rv_hat; today's 16:00 row carries a PLACEHOLDER target (the
  15:30 bar's RV) that nothing in the row's prediction depends on (features shift(1), diurnal baseline shift(1), MZ on
  prior sessions) -- `run.py --self-test <day>` is the gate on that claim. The sign rule becomes a BREAK-EVEN STRADDLE
  PRICE P* = Black-76 package price at sqrt(rv_hat) on the nearest-OTM listed strikes (SPX 5-pt grid, XSP 1-pt), so
  the card reads "BUY N XSP straddles if ask <= P*, limit at the ask, chase +5%, hold to settlement; else NO TRADE";
  month-ends buy regardless (proposal 55); N = floor(capital x f / (ask x 100)) with f 0.033 (half-Kelly, study 76)
  and 0.10 for month-ends (0.15 standing / 0.22 ceiling per study 77). Google Calendar event at 15:30 with a popup,
  idempotent per session; a FeedError or arm failure posts "NO SIGNAL" and exits 2. Yahoo's delays (verified): Cboe
  indices 15 min, CME 10 min, ^GSPC real-time -- so three input modes: ibkr (reference, seam not wired),
  free_substitute (^GSPC last-bar moments + ~15:15 Cboe prints; a MODEL CHANGE to be validated on the purchased
  history first), free_delayed (wait to ~15:45, card marked LATE). Ingest for the one-time gap: Databento GLBX.MDP3
  ohlcv-1m ES.v.0 (UTC ns bar-start; roll days dropped) and FirstRate 1-min CSVs (ET bar-start), both shifted to
  bar-END naive ET. The moment definitions in features.py are this package's (the vendor's builder is not in the
  repo) and MUST pass assert_parity vs the vendor panel on 2024-04 before any forecast is trusted. 23 tests; live/ibkr
  still 282. CARC staging for the bucket (per-bar arms only, pooled symlinked to live_feasible's) shipped:
  `bash cluster/slurm/submit_free.sh` -- SUBMITTED by the user 2026-09-23: CARC jobs 12293852 (canary), 12293853
  (free_re), 12293854 (free_lasso), 12293855 (free_score). CANNOT FINISH until the purchased history arrives: the gap rows 2024-05 -> now
  (the 65-session ladder and the 2000-day window are broken without them), the April-2024 parity gate, the 30-day
  Yahoo-vs-purchased seam gate and the 15:30 latency log, the self-test on a real past day, the free_substitute
  degradation study, the ibkr seam. Also open: the FOMC statement-dates CSV is operator-maintained (empty by design);
  the XSP 1-point strike step to verify on a live chain.
- 2026-09-23 OPERATOR: MONTH-END LONG FRACTION 0.15 (after study 77; "15% is good with me"). live/ibkr
  `month_end_long_fraction` default None -> 0.15 (None still = the stress fraction), CLI default 0.15;
  live/close_signal DEFAULT_MONTH_END_FRACTION 0.10 -> 0.15; READMEs; the two fraction-pinned tests set 0.10
  explicitly (the outlay-bound chase test) or assert 0.15. 282 + 23 tests pass.
- 2026-09-23 FREE_FEASIBLE -- RESULT (CARC 12293852-55, 40 arms in 6 min; `compare_mfiv_harlag.py --family free`,
  results/linear_subsection/free_carc/compare_free.csv; tw 2000 vs live_feasible on the 866 deck days): dropping the
  ES liquidity columns AND numobs costs QLIKE +2.1 / +2.4 / +1.8% at the close (ridge / enet / lasso; the enet
  interval [+0.0005, +0.0048] excludes zero), worse at 10-12 of 13 bars (mean +1.2 .. +1.9%); trade ridge 1.60 / 1.13
  (vs 1.90 / 1.44), enet 1.67 / 1.20 (vs 1.41 / 0.94), lasso 1.52 / 1.06 (vs 1.83 / 1.36). The liquidity group was
  the weakest bucket alone (2026-09-22) and still carries ~2% in combination. BUT the free feed CAN reproduce
  sumvolume (Yahoo's ES=F 1-minute bars carry volume); only numobs (the tick count) is out of reach -- so bucket
  `free_feasible_vol` = live_feasible minus numobs (15 cols) is added to the vixonly submit; the pair prices the tick
  count alone. If it recovers the 2%, the close-signal service switches to it.
- 2026-09-23 STAGED (CARC, human submits `bash cluster/slurm/submit_vixonly.sh`): DOES THE VIX BUCKET NEED VVIX AND
  VIX3M? On the 15:30 stamps with all three prints (2,943 sessions 2012-2024) log VIX3M is 95% the VIX level
  (log-linear slope 0.83, resid sd 0.06 -- the mean-reversion map), log VVIX 48%; beside log VIX and the last bar's log
  RV the pair adds +0.0064 of R^2 to the next bar's log RV (F 56), and the realized vol-of-VIX from the VIX's own
  30-minute path (std of the log change over the last 65 / 286 OBSERVED RTH prints, ~5 / ~22 sessions) recovers
  +0.0031 of it -- half. `experiments/build_vix_rvol.py` -> `data/vix_rvol.parquet` (vix_volofvol_5d / _22d; NOT
  "vix_rvol": the rv stem would be square-rooted -- the builder's stem check caught it; both resolve to the vix stem,
  log, no diurnal; gate dR2 +0.0031 reproduced). Buckets vix_only (1), vix_rvol (3), live_vix_only (14), live_vix_rvol
  (16), plus free_feasible_vol (15); 13-bar packs x 3 estimators at tw 2000, no pooled fits (the `none` dirs symlink
  live_feasible's -- the CARC root has no implied_vol pooled arms); `compare_mfiv_harlag.py --family vixonly` and
  `--family free` (default family byte-identical). Shipped; md5s match; the panel loads on CARC with the two columns.
  SUBMITTED by the user 2026-09-23: CARC jobs 12294973 (canary), 12294974 (vo_re, 10 packs), 12294975 (vo_lasso, 5),
  12294976 (vo_score).
- 2026-09-23 VIXONLY + FREE_FEASIBLE_VOL -- RESULT (196 arms in 7 min; `compare_mfiv_harlag.py --family vixonly` and
  `--family free`; tw 2000 vs live_feasible per-bar arms on the 866 deck days; ridge / enet / lasso). (a)
  free_feasible_vol (= live_feasible minus numobs, 15 cols): close QLIKE -0.5 / +0.2 / -0.2% (lasso interval
  [-0.0003, -0.0001] excludes zero on the better side), 13-bar mean -0.2 / -0.4 / -0.3%, trade 1.80 / 1.33, 1.47 /
  1.00, 1.85 / 1.39 vs 1.90 / 1.44, 1.41 / 0.94, 1.83 / 1.36 -- the 2% the free bucket lost was SUMVOLUME, which
  Yahoo's ES=F bars carry; the tick count numobs is worth nothing. The close-signal service forecasts with
  free_feasible_vol. (b) live_vix_only (live_feasible minus vvix, vix3m, 14 cols): close QLIKE -0.04 / +0.14 /
  +0.10%, 13-bar mean -0.4 / -0.5 / -0.1%, trade 1.86 / 1.39, 1.90 / 1.43, 1.85 / 1.38 -- VVIX and VIX3M add nothing
  measurable at the per-bar arm (the +0.0064 R^2 of the 15:30 regression does not survive the ridge); the live feed
  needs only ^VIX. live_vix_rvol (+ the VIX's own realized vol-of-vol): -0.12 / +0.03 / +0.98%, trade 1.71 / 1.24,
  1.87 / 1.40, 1.64 / 1.17 -- the proxies add nothing and cost the ridge and lasso trades. vix_only (HAR + calendar +
  vix): close QLIKE -5.9 / -0.6 / -1.7% (BETTER than the full live bucket at the close for ridge, interval [-0.018,
  +0.0012] -- the smaller design wins the 16:00 QLIKE, as the 2026-09-22 bucket table showed for implied_vol) but
  +3.3% across the 13 bars and trade 1.48 / 1.01, 1.34 / 0.86, 1.42 / 0.94 -- the moments are what trade. VERDICT:
  the service's bucket is free_feasible_vol minus vvix / vix3m (13 cols: 8 ES moments incl. sumvolume, vix, 4 FOMC),
  to be confirmed by one arm set (`free_vix_only`) before it replaces the 15-col bucket; the FirstRate purchase can be
  the VIX single (VVIX / VIX3M optional for cross-checks).
- 2026-09-23 NO FIRSTRATE NEEDED ("do we even need firstrate for the vix?"). Yahoo's free history covers the VIX gap:
  hourly ^VIX bars reach 730 days back (2023-12-07 today; the bar OPEN is the print at the hour; VVIX / VIX3M hourly
  bars are RTH-only, stamped from 09:30) and daily bars to 1990. On the vendor panel (2,995 sessions 2012-2024) the
  15:00 print in place of the 15:30 print costs the next-bar log-RV regression R^2 0.8211 -> 0.8200; the previous
  day's close 0.8125; RV_prev alone 0.7693 -- hourly prints on the gap rows lose nothing measurable, a daily carry
  little. Snapshotted before the window rolls: data/free_feed/{vix,vvix,vix3m}_1h_yahoo.parquet and _1d_yahoo.parquet
  (+ README with the pull stamp). The one-time purchase is now Databento ES 1-min inside the signup credit -- $0
  out of pocket; writeup/history_purchase_2026-09-23.md updated. TODO in the service: an ingest path for the Yahoo
  hourly files (hour opens at the :00 stamps, carried to the :30 stamps, journaled as "hourly" rows) beside the
  FirstRate one.
- 2026-09-23 CBOE GAP INGESTED FROM THE FREE YAHOO SNAPSHOTS; SERVICE BUCKET free_feasible_vol; free_vix_only STAGED
  (subagent, reviewed). live/close_signal/ingest.py: the Yahoo hourly bars place the panel's value at stamp T as the
  close of the bar ENDING at T (on the vendor overlap 2023-12-07..2024-02-12: log-error MAD 0.0000 on 629 / 270 / 315
  stamps, VIX max 0.0039 -- the vendor's print at T is the one standing just before T; the bar OPEN at T is off by MAD
  0.3%, p95 1.6%); the other half-hour stamps carry the day's last print (MAD 0.27-0.39%, p95 1.3-1.6%; a
  30-minute-stale VIX costs R^2 0.8211 -> 0.8200 on the vendor panel); stamps before the first print and days without
  bars take the previous session's daily close, never the same day's. 45,792 grid rows 2024-02-13 -> 2026-09-23 in
  state/cboe_gap_yahoo.parquet (per-row provenance) and panel_free.parquet (source yahoo_cboe_hourly). state.py bug: an
  empty store's first merge produced object-dtype columns -- cast to float. BUCKET -> free_feasible_vol (the 15-col
  bucket within 0.5% of live_feasible); free_vix_only (13 cols, minus vvix / vix3m) staged: submit_freevix.sh,
  shipped, md5s match; compare family free extended. 31 close-signal tests pass.
- 2026-09-23 FIDELITY GATES ON THE LIVE FORECAST PATH (operator's rule: no speed hacks; the regression must never need
  re-validation against the true data). GATE 1: the research arm (bar1600 / ridge / tw 2000 / live_feasible) run
  LOCALLY over the FULL vendor panel (START 0, END -1, no halo) equals the CARC arm on 1,469 rows to a max relative
  7e-12 in pred_adj, realized variance bit-identical -- the full-panel run is the path of record (run_arm(full=True));
  the chunk-with-halo variant is diagnostics only. GATE 2: the deck's rv_hat is NOT reproduced by recalibrating the
  16:00 rows alone (median 4-5% off, 89 of 866 signs differ) nor by asl.load_yhat_panel_mz -- WRONG PATH, which the
  first build had. GATE 2c: the table the research actually used -- ALL 13 per-bar arms stacked (20,044 rows,
  identical to results/spxw_pnl/yhat_sub_ridge_live_feasible.parquet to 0.0), rv_raw = the panel's own sumret2 at each
  stamp (never the arm's winsorized true_raw: 18 of 1,469 16:00 rows differ), through the deck's own loader
  asl.load_yhat_1530_mz_cached(method "mean") -- reproduces the deck's rv_hat to 0.00 on 866/866 days with every
  sign agreeing: the MZ map is fitted on the whole session's rows (fit_mask = rth), not the close alone. The service
  therefore runs the 13 bar arms each day (~1-2 min each, a few in parallel), assembles the table as
  build_subsection_yhat does, and calls the deck's loader; `run.py --gate-deck` re-runs GATE 2c on demand (fix in
  progress by a subagent).
- 2026-09-23 FREE_VIX_ONLY -- RESULT (CARC 12296964-67, 40 arms in 6 min; `compare_mfiv_harlag.py --family free`): the
  13-column bucket (8 ES moments incl. sumvolume, vix, 4 FOMC; no numobs, no vvix / vix3m) vs live_feasible at tw 2000
  on the 866 deck days: close QLIKE -0.43 / +0.11 / -0.63% (ridge / enet / lasso; every interval through zero),
  13-bar mean -0.0 / +0.2 / +0.8%, trade 1.83 / 1.36, 1.81 / 1.35, 1.91 / 1.45 vs 1.90 / 1.44, 1.41 / 0.94, 1.83 /
  1.36 -- within noise of the 16-column research bucket on every cell. VERDICT: the live feed is ES=F 1-minute bars
  and ^VIX, nothing else; the service's bucket becomes free_vix_only (BUCKET switch after the fidelity fix lands);
  the ingested VVIX / VIX3M gap rows stay as harmless columns. The three free buckets side by side (2.1 / -0.5 / -0.4%
  at the close for ridge) close the feed question: sumvolume is worth ~2%, numobs nothing, VVIX / VIX3M nothing.
- 2026-09-23 CLOSE-SIGNAL FORECAST = THE DECK, PROVEN (subagent, reviewed). GATE 1: the research arm (bar1600, ridge,
  tw 2000, live_feasible) run locally over the FULL vendor panel equals the CARC arm to 7e-12 on 1,469 rows. GATE 2:
  the deck's recalibration on the 16:00 rows alone is 4-5% off rv_hat and disagrees with sign(s) on 89 of 866 days --
  the MZ map is fitted on the whole session's rows. GATE 2c: ALL 13 per-bar arms stacked exactly as
  build_subsection_yhat does (t at microseconds, rv_raw = the PANEL's sumret2, never the arm's winsorized true_raw)
  through the deck's own loader asl.load_yhat_1530_mz_cached reproduce the research table on 20,044 / 20,044 rows
  (baseline and rv_raw exact, yhat max rel 5.5e-7 = the known cross-machine float path; bar1600 7e-12) and the deck's
  rv_hat on 866/866 days (max rel 9.6e-9), signs 866/866. live/close_signal/forecast.py now runs the 13 arms in full
  (START 0, END -1, no halo; 3 spec processes at a time, ~6-8 min on the runner, 15 min locally at 2), assembles the
  table that way and calls that loader; `run.py --gate-deck [--reuse-arms]` is the standing fidelity gate (tolerances
  yhat 1e-6, rv_hat 1e-7, the deterministic columns and the signs exact); the placeholder-invariance self-test now
  compares every row of the 13-bar table up to the session. BUCKET -> free_vix_only. 37 close-signal tests. Open: run
  the gate once on CARC for a same-machine figure.
- 2026-09-23 CALENDAR PATH, FIRST DISPATCH (workflow on main c7fb6fd, checks out the research branch; run 35944056780,
  --skip-guard after the close): guard skipped, slept 0 s, feeds fetched, the arm failed as expected without the ES
  gap rows ("train_window (2000 periods) >= chunk size (0)"), "NO SIGNAL for 2026-09-23" posted, exit 2 by design.
  The state step reported "no state change" although the NO-SIGNAL path appends a journal row -- to verify on the next
  dispatch (the runner now prints the posted event id; the operator confirms the card on the calendar). Secrets set
  through gh (GCAL_CLIENT_ID / _SECRET / _REFRESH_TOKEN, GCAL_CALENDAR_ID=primary, CLOSE_SIGNAL_CAPITAL=70000); the
  refresh token never printed. THE OPERATOR CONFIRMED THE CARD on the calendar ("I got the card") -- the Calendar path
  is closed. The bot's state commit (journal, latency, panel) did land on the branch ("no state change" was the step's
  echo before the push); the latency table read Yahoo live: ES=F 9.1 min behind (nominal 10), Cboe prints stale to
  16:14 at 21:44 ET. The journal's error text carried the spec's feature-health FAILs on the free bucket's ladder
  columns (max |z| ~22): the appended gap rows have Cboe prints but no ES moments yet; re-check after the Databento
  ingest.
- 2026-09-23 FOMC STATEMENT DATES FILLED from the Fed's calendar page (fetched, not typed): 25 statement days
  2023-12-13 .. 2026-12-09 (the second day of each two-day meeting; the 2025-08-22 notation vote excluded; 2027 to be
  added as each date is confirmed). BUG FOUND on the first fill: forecast.fomc_release_rows used np.isin(datetime64,
  [Timestamp, ...]) and matched nothing -- every FOMC day since 2023-11 would have been flagged 0 silently; fixed with
  Index.isin, three tests (statement day flags its 14:30 stamp only; a missing file gives zeros; the shipped file
  parses, is monotone, weekdays, 27-63-day gaps, starts where the vendor feed stopped). 40 close-signal tests.
