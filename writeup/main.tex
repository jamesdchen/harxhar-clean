\documentclass[12pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath, amssymb, amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{hyperref}
\usepackage[margin=1in]{geometry}
\usepackage{natbib}
\usepackage{float}
\usepackage{subcaption}
\usepackage{multirow}
\usepackage{xcolor}

% Compile-safe figure slot: renders the image if it exists, else a labelled "pending" box.
\providecommand{\treefigorpending}[2]{%
  \IfFileExists{#2}{\includegraphics[width=#1]{#2}}%
                   {\fbox{\parbox[c][1.5cm][c]{#1}{\centering\itshape pending: \detokenize{#2}}}}%
}

% Visible marker for any quantity not yet verified against the frozen battery.
% Every occurrence must be resolved or removed before submission; grep for \pending.
\newcommand{\pending}[1]{\textcolor{red}{\textbf{[pending: #1]}}}

% Joint-development marker: an open decision for the authors, not a missing
% number. The argument (the full decision note) is kept in the source for the
% authors but NOT rendered — readers see only a compact marker, not the
% inventory of experiments and options under consideration.
\newcommand{\decide}[1]{\textcolor{blue}{\textbf{[decision pending]}}}

% Campaign numbers: generated by experiments/score_unification.py at harvest.
% Before harvest every macro renders a red pending box; after, the number.
\input{generated/campaign_numbers}
% Fallbacks for arms still on the cluster; no-ops once the scorer defines them.
\input{pending_macros}

\title{\pending{working title — the block-ridge arc}}
\author{}
\date{\today}

\begin{document}

\maketitle

\begin{abstract}
\pending{abstract — written last, once the arc is frozen}
\end{abstract}

\input{sections/introduction}
\input{sections/data_prep}          % §2 data prep + preliminary descriptive analysis → OLS on HAR
\input{sections/smearing}           % §3 Duan smearing / 2nd-moment correction — frozen before any comparison;
                                    %    future LSTM-fitted correction wedges into this same slot
\input{sections/buckets}            % §4 OLS of the buckets — exogenous variables contribute
\input{sections/dense_weak}         % §5 lasso vs ridge head-to-head → 2-block ridge
\input{sections/pca_factors}        % §6 PCA factor block → volatility-gated PCA adapter (3-block ridge)
\input{sections/nonlinear_product}  % §6 trees tease the product block → 3-block ridge
\input{sections/conclusion}

\input{sections/appendix}

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
