\section{Algorithm Design}
\label{sec:algorithms}

With the feature set frozen, the estimation \emph{algorithm} becomes the design
space. This section presents the measurement instrument the rest of the paper
depends on---a causal hyperparameter-selection protocol---together with the
exact every-bar estimators that make it computable, and one proposed
nonparametric estimator whose ablations are decisive against its novel
component.

\subsection{Causal Hyperparameter Selection}
\label{sec:causal_tuning}

Section~\ref{sec:tuning_leak} showed what happens when hyperparameters are
selected on the evaluation panel. The protocol below is the replacement, and
every arm in Sections~\ref{sec:marginal} and~\ref{sec:linvsnonlin} is estimated
under it.

Every $250$ solves, each arm re-selects its own hyperparameters from a
leakage-clean forward split of its \emph{current} training window
($500$ trading days, $24{,}000$ bars):

\begin{enumerate}
  \item a fit block, taking all but the last $150$ bars of the window;
  \item a $25$-bar embargo, chosen to cover the longest moving average
    entering the features, so that no validation row shares a rolling window
    with a fit row;
  \item a $125$-bar validation tail, on which configurations are scored by
    mean squared error.
\end{enumerate}

The winning configuration is then used for the next $250$ forecasts and
discarded. No forecast's hyperparameters are ever selected using information
from that forecast's own future, and no forecast's hyperparameters are selected
on QLIKE, so the evaluation loss is never the selection loss.

Linear arms (ridge, recursive lasso, recursive elastic net) search log-scaled
penalty grids and slide warm between tunings via the exact updates of
Section~\ref{sec:exact_estimators}, cold-reseeding at each re-selection. Tree
arms search max-depth $\{3,5,8\} \times$ colsample $\{0.25, 0.5\}$ with the
learning rate pinned at the production value, fit grid cells at 300 trees and
refit the winner every bar at 500. Model refit is every bar for all arms, so
the refit-cadence asymmetry that confounded earlier comparisons is absent.

We regard this protocol, rather than any individual arm, as the section's
primary contribution: it converts ``tuning does not help here'' from an
anecdote about one search into a claim with a defensible causal structure.

\subsection{Exact Every-Bar Estimators}
\label{sec:exact_estimators}

Per-bar refitting over $218{,}934$ bars, crossed with 45 arms and a periodic
hyperparameter search, is only feasible if the updates are exact and
incremental.

\paragraph{Rank-1 rolling least squares.} Adding and dropping one observation
from a rolling-window least-squares fit are rank-1 modifications of the
Gram matrix, handled by the Sherman--Morrison identity. This makes the
every-bar OLS incumbent exact and computable at panel scale, and it is what
allows the benchmark to be a genuinely per-bar refit rather than a cadence
approximation.

\paragraph{Online elastic-net homotopy.} The $\ell_1$ analogue of the same
idea. As the window slides, the elastic-net solution path is updated by
homotopy continuation \citep{Garrigues2008}: the active set and the signs of
the active coefficients change at a finite number of breakpoints, between
which the solution is piecewise linear in the perturbation. This delivers the
exact recursive lasso and elastic-net arms rather than warm-started
approximations, so that the ``selection loses to shrinkage'' finding of
Section~\ref{sec:tuning_nonlever} is not confounded with optimisation error.

\paragraph{Block ridge.} Where the design partitions naturally---backbone
versus exogenous block---separate penalties per block are solved jointly,
which is what allows the backbone to be lightly penalised while the wide
exogenous block is shrunk hard.

\subsection{Spectral \texorpdfstring{$k$}{k}-Nearest Neighbours}
\label{sec:spectral_knn}

The proposed estimator asks whether the dense-but-weak regime rewards analog
retrieval: embed trailing residual trajectories with Laplacian eigenmaps
\citep{BelkinNiyogi2003}, retrieve past episodes that are near in the embedded
geometry, and forecast by locally averaging their outcomes. The design has four
axes: the residualiser (OLS-anchored or identity), the trajectory window $W$,
the embedding dimension $d$ (with $d = 0$ denoting passthrough, i.e.\ no
embedding), and the neighbourhood size $k$.

\subsubsection{The embedding hurts on every matched cell}

\begin{table}[H]
\centering
\caption{Embedding ablation. Each row is a matched pair differing only in
whether the Laplacian-eigenmap step is applied ($d = 6$) or bypassed
($d = 0$). Shared panel, $n = 218{,}934$. Positive $\Delta$ means the embedding
is worse. It is worse in all eight matched cells.}
\label{tab:knn_ablation}
\begin{tabular}{llrrr}
\toprule
Window & Config & $d = 0$ & $d = 6$ & $\Delta$ \\
\midrule
$W = 12$ & OLS-anchored, $k = 100$ & 0.13219 & 0.13364 & $+0.00145$ \\
$W = 24$ & OLS-anchored, $k = 100$ & 0.13218 & 0.13399 & $+0.00182$ \\
$W = 12$ & OLS-anchored, $k = 25$  & 0.13405 & 0.13676 & $+0.00270$ \\
$W = 24$ & OLS-anchored, $k = 25$  & 0.13397 & 0.13701 & $+0.00304$ \\
$W = 12$ & identity, $k = 25$      & 0.14173 & 0.15993 & $+0.01820$ \\
$W = 12$ & identity, $k = 100$     & 0.14236 & 0.16213 & $+0.01977$ \\
$W = 24$ & identity, $k = 100$     & 0.14539 & 0.16874 & $+0.02336$ \\
$W = 24$ & identity, $k = 25$      & 0.14475 & 0.16872 & $+0.02397$ \\
\bottomrule
\end{tabular}
\end{table}

The manifold-embedding step---the novel component of the design---hurts on all
eight matched configurations, by between $0.00145$ and $0.02397$, and the
damage is an order of magnitude larger without the OLS anchor than with it.

\subsubsection{The cause is supervision, not extension}
\label{sec:knn_metric_race}

Earlier drafts attributed the failure to out-of-sample Nystr\"om extension
error: new points cannot be placed on the learned manifold accurately enough
for retrieval in the embedded space to beat retrieval in the ambient space.
That explanation is intuitive, it was never tested, and it is wrong.

The eigenmap is defective on two axes at once, and they are separable. It is
\emph{nonlinear and defined on the observation set}, so a test bar has to be
extended in---the extension story. It is also \emph{built from $X$ alone}, in a
panel where the leading variance directions carry almost none of the
predictable variation in $y$: the first principal component holds $96.2\%$ of
the design's variance and $0.02\%$ of the explained target, while principal
components $6$ through $20$ hold $0.18\%$ of the variance and $79.9\%$ of the
explained target. Adding a linear
principal-component arm at the same dimension separates the two, because a PCA
projection is an exact linear map that applies to a test point verbatim and has
no extension step of any kind.

Table~\ref{tab:knn_metric_race} holds the anchor, the candidate pool
($48{,}579$ training views), the evaluation rows ($24{,}288$, fixed by a rule
that inspects no arm) and the neighbourhood grid constant, and varies only the
retrieval coordinate.

\begin{table}[H]
\centering
\caption{Retrieval-metric race, $W = 24$, $k = 100$. All arms share the
HAR-12 OLS anchor, the pool, the evaluation rows and the neighbourhood size;
only the coordinate in which neighbours are found differs. $\Delta$ and $t$ are
matched-row Diebold--Mariano against the ambient arm, Holm-corrected across the
24-cell grid. Positive $\Delta$ means the arm is worse.}
\label{tab:knn_metric_race}
\begin{tabular}{llrrrl}
\toprule
Coordinate & Type & $d$ & QLIKE & $\Delta$ & $t$ \\
\midrule
residual path only & ambient, no exog & 24 & 0.16347 & $-0.00976$ & $-11.3$ \\
path $+$ state & ambient & 516 & 0.17323 & --- & --- \\
operator triplets & supervised & 6 & 0.17339 & $+0.00015$ & $+0.3$ \\
operator scores & supervised & 5 & 0.17554 & $+0.00230$ & $+5.0$ \\
principal components & unsupervised, linear & 6 & 0.18002 & $+0.00678$ & $+17.5$ \\
Laplacian eigenmap & unsupervised, nonlinear & 6 & 0.18154 & $+0.00831$ & $+18.2$ \\
\midrule
no retrieval (anchor) & --- & --- & 0.18322 & & \\
\bottomrule
\end{tabular}
\end{table}

Three readings follow, and $W = 12$ reproduces the ordering.

\paragraph{Extension error is not the mechanism.} PCA at $d = 6$ cannot have
extension error, and it still loses $+0.00678$---four fifths of the eigenmap's
$+0.00831$. Whatever the eigenmap's nonlinearity costs, it is a minority of the
damage.

\paragraph{Supervision is the mechanism.} The supervised coordinate at the
\emph{same} dimension $d = 6$---the trailing-path score together with
$\sigma_k\, u_k' Z v_k$ for the top five singular triplets of the train-only
fitted exogenous operator---is statistically indistinguishable from the full
$516$-dimensional ambient view ($+0.00015$, $t = +0.3$, Holm-adjusted
$p = 1.0$), while the unsupervised compressions of that dimension lose
decisively. The compression budget was never too small; it was spent in the
wrong directions. The operator's singular vectors have the incidental virtue of
living on the design axes---channels and lags---so they are fixed a priori,
finite, and require no out-of-sample extension at all.

\paragraph{The exogenous block is a net cost to a distance.} Matching on the
residual path alone, discarding all $492$ exogenous ladder coordinates, beats
the composed view by $0.00976$ ($t = -11.3$). The same block that a fitted
linear map profits from degrades a metric, because a distance weights every
coordinate by its scale rather than by its relevance. This is the
dense-but-weak fact in its sharpest form: with many weakly informative
channels, \emph{fitting} and \emph{measuring similarity} are not the same
operation, and only the first tolerates breadth.

The revised claim, then, is narrower than the paper previously made and
better supported: unsupervised geometry fails in this regime, at any
dimension tested and whether linear or nonlinear. Supervised geometry does
not fail---it merely fails to \emph{improve} on the ambient view, which is
the honest ceiling on the idea.

\subsubsection{What is load-bearing is not novel}

The same table identifies what does the work.

\begin{itemize}
  \item \textbf{The OLS anchor is load-bearing}: at $d = 0$, replacing the
    identity residualiser with the OLS anchor is worth between $0.00768$ and
    $0.01321$ across matched cells---five to nine times the size of any
    model-class effect in this paper.
  \item \textbf{Large-neighbourhood averaging is load-bearing, up to a point}:
    moving from $k = 25$ to $k = 100$ is worth $\approx 0.0018$ in the anchored,
    unembedded cells. Per-bar inference on the campaign tile confirms the effect
    and locates its limit: a neighbourhood of $8{,}000$ beats one of $200$ by
    $0.0204$ ($t = -6.59$), but $16{,}000$ is $0.0029$ \emph{worse} than
    $8{,}000$ ($t = 3.74$), both Holm-significant
    (Section~\ref{sec:tile_inference}). The optimum is a large but finite
    neighbourhood, so the correct statement is not that more averaging is
    always better. Heavy local averaging rather than sharp local structure is
    what the regime rewards---the nonparametric restatement of dense-but-weak.
  \item \textbf{The embedding is not}, as above.
\end{itemize}

The surviving estimator is therefore an OLS-anchored, unembedded, heavily
averaged analog search. It is a reasonable method and it is not novel. We
report this as the section's finding rather than burying it: in the
dense-but-weak regime nonparametric flexibility is a cost to be justified, not
a default.

\subsubsection{The headline cell is a selected minimum}
\label{sec:knn_selection}

The best kNN arm reaches QLIKE $0.13096$ against the incumbent's $0.13415$, at
DM $t = -17.6$. Three qualifications belong with that number, and earlier
drafts of this paper carried none of them.

\paragraph{It is the minimum of 32 cells.} The kNN family contributes 32 arms
to the battery, of which 8 beat the incumbent. Quoting the minimum with an
unadjusted DM statistic, on the same panel used to select it, is a
winner's-curse figure. Family-wise correction across the battery does not
dissolve it---its $p$ survives Holm across all 97 arms
(Section~\ref{sec:multiplicity_results})---but a $p$-value adjustment does not
address selection: the issue is that the cell was chosen for being the minimum,
not that its individual test is too liberal. The honest presentations remain
either a pre-registered cell or a confirmation on held-out data.
\pending{holdout confirmation of the winning kNN cell, or a pre-registration
statement identifying it as chosen ex ante}

\paragraph{Its bucket is the one that survived.} The winning cell sits on the
\texttt{market\_vw} bucket with an exogenous mask; sibling buckets failed as
singular under the same configuration. Until that failure is diagnosed, the
bucket ranking within the kNN family carries no weight, and the choice of
\texttt{market\_vw} cannot be read as an information finding.

\paragraph{It does not trail every parametric arm.} At $0.13096$ the winning
kNN cell numerically beats \textbf{25 of the 45} causally tuned parametric
arms, including the baseline LightGBM ($0.13118$), baseline XGBoost
($0.13163$), and all three baseline linear arms ($0.13445$, $0.13471$,
$0.13475$). An earlier draft asserted that it trails every parametric arm; that
is false against our own battery.

Applying the pairwise bound of Section~\ref{sec:pairwise} sharpens the picture
in both directions. The kNN cell is decisively \emph{worse} than the best arms
on the informative buckets---$|t| \ge 6.96$ against \texttt{lgbm/all\_features}
and $|t| \ge 6.22$ against \texttt{ridge/all\_features}---and decisively
\emph{better} than baseline ridge ($|t| \ge 14.49$). Against baseline LightGBM,
where the raw gap is only $0.00022$, the comparison is \textbf{inconclusive}
($|t| \ge 0.30$). So the defensible claim is narrower than either draft: the
anchored analog search beats the linear arms on the backbone feature set and
loses to the tree and linear arms on the informative buckets, with the
tree-on-backbone comparison unresolved.

\subsection{Summary}
\label{sec:algo_summary}

With features frozen, exactness and anchoring matter more than expressiveness.
The causal-tuning protocol makes the paper's central negative result about
hyperparameters defensible; the rank-1 and homotopy updates make an every-bar
benchmark computable at panel scale; and the analog-retrieval experiment
resolves into a clean decomposition in which the anchor and the averaging pay
and the manifold does not. The novel component of the proposed estimator is
the component that fails, and we report it at the same altitude as the parts
that work.
