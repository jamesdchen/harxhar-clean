# writeup/ — master index (2026-08-06, alpha-manifestation campaign close)

## The four live documents (read in this order)

| Doc | What it is |
|---|---|
| `CLAIMS_LEDGER_2026-08-06.md` | **The graded index.** Every claim with its tier, number, and § citation. Start here. |
| `alpha_manifestation_findings_2026-08-04.md` | **The full record**, §§0–40: every arm pre-registered with recorded leans, every result, every casualty. The reproducibility block at the end maps §→command. |
| `paper1_fold_plan_2026-08-06.md` | The map for merging this campaign into Paper 1 (with the post-§37–40 addendum). |
| `future_work.md` | AM-01…AM-16 backlog. AM-01 (panel refresh 2024-03→present) is the biggest single lever; AM-13 + LSTM head are cluster-side. |

## Paper 1 (in this directory)

`main.tex`, `sections/`, `references.bib`, `figures/`, `stats/`, plus table artifacts
(`causal_tune_battery_table*`, `master_table_doc.tex`, `metrics_table_*.csv`) and their
build scripts (`merge_har_ladder_rows.py`, `plot_har_ladder_qlike_3d.py`).
`paper_restructure_2026-08-01.md` (S1–S5 arc) and `PAPER_CRITIQUE_2026-08-02.md` (the
critique the session resolved) are the paper's planning docs.

## Specs and handoffs still in force

- `om_0dte_atopen_export_spec.md` — the 3-tier VRP data export spec (blocks AM-02).
- `SESSION_HANDOFF_2026-08-01B.md` — last pre-campaign handoff (context for §0).

## The final model, one paragraph

719 columns per-bar (backbone 27 @α=1 | exog 516 @3e3 | xsec 36 @3e3 | frozen-2005 products
100 @3e4 | transmission 40 @3e3 — width measured, plateau 30–80), 250d window, 1-day embargo;
QLIKE composed with the conditional smear (means + leverage + probe, 10 cols; calendar +DOW
+short-day pending AM-15): **0.12303** (≈ −7.2% vs HAR). Channel law: the mean is dense and
fast; the variance is means-sufficient on the modern regime (its historical extra span —
calendar + slow aggregates — died 2022+ except the calendar fragment). Sixteen casualties.
Full grading in the ledger.

## `archive/`

Pre-campaign material (June–July 2026): session handoffs, campaign runbooks, exploratory
findings (regime MoE, intraday regimes, explainability audit, idea-to-trade pipeline,
prop-venue survey, GPU runbook, data-buy specs). Superseded, kept for provenance.
