Pre-hpc-agent-consumption snapshot of harxhar.

Contents: notebooks (`@register_run` experiments + pipeline + audits + scripts), input parquets (`all30min/`), tracked results summaries, writeup. Run `hpc-agent install` to generate `src/`, `.hpc/`, and the rest.
